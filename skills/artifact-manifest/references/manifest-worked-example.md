# Manifest Worked Example

STUB — filled by sub-issue #256 (Content: artifact-type-catalog and manifest-worked-example references).

A full, populated `_manifest.json` instance for the running product (Data Estate Mapping and Compliance Intelligence), illustrating the shape `SKILL.md`'s Per-Product Manifest Instance section describes: the `_meta` block, an approved ADR entry, a superseded user-story entry (with `superseded_by`), and — per the Content sub-issue's scope — additional entries illustrating a `draft`-status artifact and a source-tree artifact (a Go service or React component) to show how the "one manifest entry per meaningful unit" rule applies to directory-shaped artifacts, not just single Markdown files. This file is illustrative and populated — distinct from `assets/manifest-instance-template.json`, which is the empty skeletal shape a command copies in when initializing a new product's manifest.

## Worked Example

<!-- Full JSON worked example goes here. -->
