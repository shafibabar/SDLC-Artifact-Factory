# OpenAPI Spec Structure — Full Worked Example

The complete top-to-bottom worked example of an `api/openapi.yaml` file, moved here from `SKILL.md` as illustrative reference material. Self-contained — loadable without reading `SKILL.md` first.

*Stub — to be filled by sub-issue #238. Brief: the full worked OpenAPI 3.1 YAML example (the skeleton previously inline in `SKILL.md`: `openapi` version, `info`, `servers` with the `tenantId` multi-tenancy variable, global `security`, `tags`, a complete `paths` entry for `/data-assets` `GET`, and `components.schemas`/`components.securitySchemes`), extended with a `components/schemas/Operation` entry reflecting the Long-Running Operation shape documented in `references/advanced-resource-patterns.md` — write this after that content sub-issue lands so the two stay field-for-field consistent.*
