# API Contract Summary Output Format Template

The full fill-in template for a service's API contract summary artifact. Self-contained — loadable without reading `SKILL.md` first.

*Stub — to be filled by sub-issue #238. Brief: the annotated version of the `api-contract-summary.md` template (every placeholder explains what belongs there and why — `name`, `product`, `service`, `version`, `phase`, `created`, `owner`, `openapi-spec` frontmatter fields; the `Endpoints` table with Method/Path/Command-or-Read-Model/Auth/Idempotency columns; the `Breaking Change Log` and `Consumer Registry` sections), matching `acceptance-criteria`'s `references/output-format-template.md` exact convention — pointing to `assets/api-contract-summary-template.md` for the literal fill-in-and-go copy once that sub-issue builds it, and to `scripts/scaffold-api-contract-design.sh` as the generation shortcut.*
