# Changelog

All notable changes to the SDLC Artifact Factory are documented here.

Format: [Semantic Version] — Date — Description

---

## [Unreleased]

### Added
- 2026-07-31 — **Python backend Tier 3 — full parity** (epic #758): the final 9 `python-*` skills, no research phase (grounded in the Chunk 62 tech-stack research + testing cluster + `go-*` siblings). Skills: `python-grpc-handler` (grpcio), `python-amqp-publisher`/`python-amqp-consumer` (aio-pika), `python-mongodb-repository` (motor), `python-e2e-test`, `python-mutation-test` (mutmut/cosmic-ray), `python-load-test` (locust/k6), `python-chaos-test` (toxiproxy), `python-performance-test` (pytest-benchmark). 18 new references/ files, 9 contract tests. Repo grew 177→186 skills, 177→186 contract tests. **MILESTONE: 28 `python-*` skills now match the 28 `go-*` skills — full FastAPI-vs-Go backend parity** across structure, persistence (Postgres+Mongo), messaging (Kafka-API+AMQP), APIs (REST+gRPC), and the full test pyramid. Python is now a first-class backend stack alongside Go, per-product selectable at `/sdlc-start`. The three-tier Python suite (epics #713/#739/#758) is complete.
- 2026-07-31 — **Python backend Tier 2** (epic #739, research-first): messaging + core testing, the second Python slice from the #695 spike. **Phase 1** (#745): `research/testing/python-testing-with-pytest-okken.md` (pytest fixtures/parametrize/markers, pytest-asyncio, testcontainers-python, fakes-over-mocks). **Phase 2** (#740–#748): 8 new `python-*` skills, all v1.0.0, backend-engineer, each mirroring its `go-*` sibling — `python-event-consumer` (aiokafka, Semaphore-I/O vs ProcessPool-CPU, DLQ), `python-event-publisher` (asyncio outbox poller, `FOR UPDATE SKIP LOCKED`), `python-async-concurrency` (asyncio.TaskGroup, GIL, ProcessPoolExecutor — the widest divergence), `python-unit-test` (pytest + pytest-asyncio + fakes-over-mocks), `python-integration-test` (testcontainers-python), `python-contract-test` (schema-based, no Pact broker), `python-performance-optimization` (py-spy/scalene/tracemalloc), `python-makefile` (uv/ruff/mypy/pytest). 16 new references/ files, 8 contract tests. Repo grew 169→177 skills, 169→177 contract tests; 19 `python-*` skills total (Tier 1 + Tier 2). Python Tier 3 (parity) remains a follow-on.
- 2026-07-31 — **Python backend Tier 1** (epic #713, research-first): the MVP-backend slice from the #695 spike, giving a usable Python path alongside Go. **Phase 1** (#720): `research/software-architecture/architecture-patterns-with-python-percival-gregory.md` ("Cosmic Python" — DDD-in-Python: Repository, Unit of Work, Service Layer, domain events, ports-and-adapters). **Phase 2** (#714–#725): 11 new `python-*` skills, all v1.0.0, owner backend-engineer, FastAPI + asyncpg, each mirroring its `go-*` sibling — `python-project-structure` (import-linter Layers, `typing.Protocol` ports), `python-service-skeleton` (FastAPI `lifespan`), `python-domain-model` (frozen dataclass/Pydantic), `python-repository-pattern` (`asyncpg` + FakeRepository, same CAS/outbox/tenant rules), `python-service-layer` (`asyncio.TaskGroup` + Unit of Work + `contextvars`), `python-fastapi-handler` (Pydantic boundary validation), `python-middleware` (Starlette chain), `python-error-handling` (`raise ... from`, catch-once-at-edge), `python-migration` (Alembic over SQL), `python-openapi-codegen` (code-first, the direction-flip vs Go + conformance-check fix), `python-dockerfile` (multi-stage uv/pip, non-root, distroless-python). Each documents the honest Python-vs-Go divergences (no compiler `internal/`, weak encapsulation, GIL, code-first OpenAPI). 22 new references/ files, 11 contract tests. Repo grew 158→169 skills, 158→169 contract tests. Python Tiers 2 (messaging+testing) and 3 (parity) remain as future campaigns.
- 2026-07-31 — **Tech-stack gap closure** (epic #678, research-first): closes coverage gaps for a target product stack (MongoDB, RabbitMQ, Netflix Conductor, gRPC, Kong). **Phase 1** (#679–#683): 5 research files — MongoDB: The Definitive Guide, RabbitMQ in Depth, Microservices Patterns (Richardson), gRPC: Up and Running, Kong/API-gateway patterns. **Phase 2** (#684–#693): 10 new skills, all v1.0.0 under skill-authoring-standards — `document-data-modeling`, `go-mongodb-repository`, `message-broker-selection`, `go-amqp-publisher`, `go-amqp-consumer`, `workflow-orchestration`, `conductor-workflow-authoring`, `grpc-contract-design`, `go-grpc-handler`, `api-gateway-design`. The AMQP skills mirror the Kafka-API `go-event-*` pair; `go-grpc-handler` mirrors `go-chi-handler`; `grpc-contract-design` mirrors `api-contract-design`. Repo grew 148→158 skills, 148→158 contract tests. **Follow-ons (epic now fully closed):** #694 refactored `opentofu-module` (v1.2.0, +`references/cloud-provider-modules.md`, body 206→197) and `environment-config` (v1.2.0, +`references/cloud-environments.md`) with GCP/AWS/Azure provider references; #695 delivered a Python `python-*` skill-suite scoping proposal (FastAPI, ~26 skills tiered) — the Python build is a separate campaign pending approval.

### Changed
- 2026-07-31 — **Agent refactor campaign started** (epic #777): the 13 agents, written before this session's skill work, are being brought current with the refactored/new skill roster and research — one agent at a time in SDLC phase order, each keeping the canonical frontmatter schema and gaining a Behavioral Directives index (numbered principles citing owning skills, per the platform-engineer template) and an acceptance test. **Agent 1/13 — `product-strategist`** (#778, v1.1.0 → 2.0.0): added `user-persona`/`jtbd-analysis`/`impact-mapping` to its skills (13 total); Behavioral Directives grounded in the v2.0.0 strategy skills (JTBD/Mom-Test discovery, Dunford positioning + Moore chasm, outcome-not-output KRs, mission-vs-vision, Lean Canvas); reconciled the methodology table; new `tests/agents/product-strategist.acceptance.sh` — **written and passing** (produces a real vision + mission, asserts the mission-vs-vision distinction end-to-end).
- 2026-07-31 — **Body-length polish** (GitHub issue #661, sub-issues #662–#669): second-split pass bringing the 8 skills over the ~200-line soft guideline down to ≤200. Content-move MINOR bumps: `alerting-rules-design` (280→200), `api-contract-design` (265→199), `react-observability` (238→200), `kubernetes-manifest` (237→196), `access-control-model` (215→185, decision tables preserved per the Chunk 35 rationale), `beta-program-design` (215→184), `helm-chart` (214→184). Full split: `zero-trust-design` (258→100, v2.0.0) — it had a contract test but no references/, so references/ was created (zero-trust-principles.md + enforcement-and-mesh.md), the description rewritten as a trigger surface, and the test updated to probe references/-only content. 9 skills remain in the 201–210 band (all with references/ + tests, within the "roughly 200" guideline) — optional future trim, not a defect.

### Added
- 2026-07-31 — **Final audit closeout** (GitHub issue #640, sub-issues #641–#650): the last 10 skills that had neither references/ nor a contract test, all rebuilt to v2.0.0 under skill-authoring-standards. Bodies reduced from 125–256 down to 81–183; 19 new references/ files; 10 contract tests. Skills: `distributed-tracing-design`, `event-storming-facilitation` (Brandolini three levels + color grammar), `feedback-template` (Mom-Test question design), `mission-statement`, `prometheus-metrics-design` (RED/USE + cardinality budget + histogram buckets), `risk-register` (Fairbanks risk-driven method), `sdlc-config-management` (field reference derived from the real `schemas/sdlc-config.schema.json`), `typescript-types` (discriminated unions + parse-don't-validate), `uat-scenario` (Spec-by-Example Given/When/Then), `ubiquitous-language` (Evans UL practice + drift detection). **MILESTONE: all 148 skills now have a contract test (0 gaps) — the skill-authoring-standards audit is fully closed.** Contract-test coverage over the campaign (Chunks 52–60): 63/142 → 148/148.
- 2026-07-31 — **Medium fixes** (GitHub issue #625, sub-issues #626–#632): 7 skills that already had references/ but lacked a contract test. Test-only additions (no body/version change): `methodology-review`, `requirements-analysis`, `roadmap-authoring`, `stakeholder-mapping`, `competitive-analysis`. Split + test: `gtm-strategy` (236→199 body, v2.0.0, positioning/segment material moved to references/) and `ui-component-spec` (221→200 body, v2.1.0 MINOR). 138 of 148 skills now have contract tests. This closes the skill-authoring-standards audit's named clusters and its has-references-no-test list; 10 skills remain without a test (all also lacking references/, needing a full split+refs+test): distributed-tracing-design, event-storming-facilitation, feedback-template, mission-statement, prometheus-metrics-design, risk-register, sdlc-config-management, typescript-types, uat-scenario, ubiquitous-language.
- 2026-07-31 — **UX cluster refactor** (GitHub issue #618, sub-issues #619–#621): all 3 UX skills (ux-architect owned) rebuilt to v2.0.0 under skill-authoring-standards. Bodies reduced from 221–235 down to 148–191; 6 new references/ files; 3 contract tests. Skills: `ux-flow-design` (235→179, task-flow/user-flow/wireflow distinction + mandatory error/empty/edge states), `information-architecture` (223→148, four IA systems + card sorting open/closed + tree testing), `user-journey-mapping` (221→191, Stickdorn service-blueprint extension with front-stage/back-stage across the line of visibility). All account for the repo's microfrontend architecture (flows/IA cross fragment boundaries; shell owns global navigation). Research: Stickdorn *This Is Service Design Doing* + Baker + Olsen, supplemented by stable public UX/IA practice. **This completes all HIGH-priority clusters from the skill-authoring-standards audit.** 131 of 148 skills now have contract tests (up from 128).
- 2026-07-31 — **Discovery/Strategy cluster refactor** (GitHub issue #599, sub-issues #600–#608): all 9 discovery/strategy skills (product-strategist + requirements-analyst owned) rebuilt to v2.0.0 under skill-authoring-standards, grounded in the existing `product-strategy-and-gtm` + `requirements-and-user-stories` research (no research phase needed). Bodies reduced from 173–275 lines down to 123–194; 18 new references/ files; 9 contract tests. Skills: `business-model-canvas` (275→146, Osterwalder 9 blocks + Lean Canvas variant), `okr-authoring` (216→123, outcome-not-output KRs + 0.0–1.0 grading), `epic-definition` (208→158, epic-to-story decomposition), `story-mapping` (199→123, Patton backbone/walking-skeleton/release-slicing), `user-story-writing` (189→194, Cohn INVEST + SPIDR splitting), `moscow-prioritization` (198→142, ~60% Must-have capacity ceiling + Kano relation), `impact-mapping` (184→146, Adzic Why/Who/How/What with Impacts-as-behavior-change), `jtbd-analysis` (181→187, job-statement format + functional/emotional/social dimensions + Mom Test interviewing), `user-persona` (173→129, proto-vs-research persona + evidence-based grounding). Worked examples anchor to the repo's Data Steward and Compliance Officer personas. 128 of 148 skills now have contract tests (up from 119).
- 2026-07-31 — **Data cluster refactor** (GitHub issue #576, sub-issues #577–#587): all 11 data skills (data-architect + data-engineer owned) rebuilt to v2.0.0 under skill-authoring-standards, grounded in the existing `data-governance-and-mdm` + `data-and-analytics` research (no research phase needed). Bodies reduced from 157–376 lines down to 119–209; 32 new references/ files; 11 contract tests. Skills: `data-pipeline-implementation` (376→119, idempotent-write dedup + DLQ routing + lineage emission), `dashboard-specification` (297→153, Knaflic chart selection + per-widget metric definitions), `data-model-design` (223→169, normalization + Kimball dimensional/SCD types + PostgreSQL DDL), `reporting-spec` (218→209), `data-pipeline-design` (217→200, batch/stream/micro-batch topology + fault-tolerance), `data-quality-rules` (216→128, DAMA six DQ dimensions + remediation), `canonical-data-model` (203→163, MDM match-merge + survivorship/golden-record), `data-retention-policy` (200→187, crypto-shredding + legal-hold precedence), `data-classification` (198→141, inherit-max propagation + classification-to-control mapping), `data-storytelling` (194→189, Knaflic decluttering/preattentive attributes/Big Idea), `data-lineage-design` (157→134, granularity decision + OpenLineage + Apache AGE graph). Cross-links into privacy-design's PII lifecycle and access-control-model reinforced. 119 of 148 skills now have contract tests (up from 108).
- 2026-07-31 — **Security cluster campaign** (GitHub issue #551), run research-first because `research/security/` had only `secure-by-design.md`. **Phase 1** (sub-issues #552–#557): 6 security/compliance books synthesized into `research/security/` — Shostack *Threat Modeling*, Google *Building Secure and Reliable Systems*, Gilman & Barth *Zero Trust Networks*, Janca *Alice and Bob Learn Application Security*, Dennedy et al. *The Privacy Engineer's Manifesto*, Helz/Willis et al. *Investments Unlimited*. **Phase 2** (sub-issues #564–#569): all 6 security skills rebuilt to v2.0.0 under skill-authoring-standards, grounded in that research — `security-implementation` (395→170 body, 4 refs, the repo's largest skill), `compliance-verification` (275→135, 3 refs), `compliance-design` (245→200, 3 refs), `security-architecture` (200→157, 3 refs), `threat-modeling` (191→171, 3 refs), `privacy-design` (203→183, 3 refs). Notable new content: threat-modeling rebuilt around the Four-Question + STRIDE-per-element + DFD/trust-boundary method; compliance-design gained the previously-absent **Separation of Duties** control (author≠approver, CI-enforced with attestation) + a gate/monitor/record enforcement-mode column; compliance-verification gained digest-pinned attestations, an explicit control-gate stage, and **Continuous Control Monitoring** (extends verification past the deploy boundary for SOC 2 Type II); privacy-design gained Privacy by Design's 7 principles + a Domain Primitive making raw-PII logging a compile error; security-architecture gained the four design principles + zero-trust (Linkerd mTLS = transport identity only, ABAC = the authorization decision). 19 new references/ files, 6 contract tests. 108 of 148 skills now have contract tests (up from 102).
- 2026-07-31 — **Architecture cluster refactor** (GitHub issue #538, sub-issues #539–#544): all 6 architecture skills rebuilt to v2.0.0 under skill-authoring-standards. Bodies reduced from 212–295 lines down to 130–204 lines; 19 new references/ files added; 6 contract tests added. Skills: `event-driven-patterns` (choreography-vs-orchestration decision, Saga with compensating transactions and pivot transaction, idempotent-consumer dedup, Transactional Outbox/CDC/DLQ, Go+Redpanda implementation), `integration-design` (sync-vs-async framework, Circuit Breaker/Timeout/Retry/Bulkhead resilience patterns, Anti-Corruption Layer, worked cross-BC example), `component-diagram` (C4 L3 notation, Clean Architecture dependency-direction rules mapped to Go packages), `container-diagram` (C4 L2 notation — **resolved the Clements-flagged component-and-connector vs. allocation viewtype conflation**, separating the runtime C&C view from the deployment view via a dedicated `references/viewtype-distinction.md`), `system-context-diagram` (C4 L1 notation, external-actor and system-boundary identification procedure), `multi-tenancy-design` (three isolation models, per-layer enforcement rules, per-tenant physical-isolation stamp/provisioning model). Research base: Clements, Richards/Ford, Newman, Kleppmann, Martin, Xu, Geewax. 102 of 148 skills now have contract tests (up from 96).
- 2026-07-31 — **Domain-modeling cluster refactor** (GitHub issue #521, sub-issues #522–#529): all 8 domain-modeling skills rebuilt to v2.0.0 under skill-authoring-standards. Bodies reduced from 223–330 lines down to 117–206 lines; 27 new comprehensive references/ files added; 8 contract tests added. Skills: `domain-event-catalog` (event format envelope, Outbox/CDC mechanics, versioning strategy, catalog template), `bounded-context-mapping` (BC discovery via linguistic fracture lines, BC definition artifact, Context Map template, service decomposition), `context-map-patterns` (all 8 Evans/Vernon integration patterns with political dynamics and Go implementation, pattern selection decision tree, worked 3-BC example), `command-catalog` (Command envelope spec with idempotency key, structural vs. business-rule validation layers, Command-to-Aggregate mapping, catalog template), `cqrs-pattern` (3 CQRS variants with Khononov pattern mapping, synchronous/event-driven/CDC projectors, full Go implementation), `read-model-design` (Read Model design document format, denormalization patterns, version-field consistency, pgx Go implementation), `event-schema-design` (CloudEvents 1.0 spec, schema versioning with upcasting, Apicurio Registry integration, Go event structs), `domain-storytelling` (pictogram notation spec, full facilitation procedure, 2 worked stories extracting UL terms). 96 of 148 skills now have contract tests (up from 88).
- 2026-07-31 — **DevOps & Platform Engineering research campaign** (GitHub issue #461, sub-issues #462–#490, PR #461 parent): 6 new skills created and 22 skills + 1 agent refactored from 9-book research cluster (`research/devops-and-delivery/`). **New skills**: `dora-metrics` (Four Key Metrics — operational definitions, cluster thresholds, Grafana instrumentation spec, delivery-performance dashboard), `kubernetes-workload-patterns` (controller-type decision table: Deployment/StatefulSet/DaemonSet/Job/CronJob/Rollout/KEDA; Init Container/Sidecar/Adapter/Ambassador composition patterns), `incident-management` (blameless postmortem artifact, SEV taxonomy, contributing-factors discipline, just-culture principle), `platform-engineering-design` (TVP, golden paths, DX measurement, service catalog, X-as-a-Service interaction mode), `gitops-workflow` (four GitOps principles, Flux CRD model, SOPS vs Sealed Secrets decision, App of Apps pattern), `progressive-delivery` (deploy/release distinction, canary vs blue-green vs feature-flag selection framework, Argo Rollouts cookbook). **Refactored skills**: `ci-pipeline` (trunk-based dev policy + Andon cord + Concourse CI comparative framing), `environment-config` (kind local cluster + Projected Volume pattern), `slo-definition` (DORA delivery axis + platform-as-product SLOs), `kubernetes-manifest` (Init Container + CPU/memory asymmetry + Argo Rollout CRD + terminationGracePeriodSeconds rule), `cd-pipeline` (Flux CRD composition model + CD vs Continuous Deployment distinction), `health-check-design` (terminationGracePeriodSeconds alignment + deploy-then-verify), `nfr-specification` (Delivery NFRs + Platform DX NFRs), `runbook-authoring` (blameless postmortem bridge + toil threshold), `canary-deployment` (Argo Rollouts AnalysisTemplate), `blue-green-deployment` (Argo Rollouts blueGreen strategy), `secrets-management` (SOPS + Sealed Secrets GitOps layer), `helm-chart` (workloadType selector), `opentelemetry-instrumentation` (Sidecar vs DaemonSet Collector decision), `disaster-recovery-plan` (weekly chaos experiments), `alerting-rules-design` (postmortem-driven tuning loop), `structured-logging-design` (Kubernetes logging pipeline), `dockerfile-patterns` (kind load docker-image workflow), `go-project-structure` (12-factor compliance checklist), `value-stream-map` (DORA cluster benchmarks), `metrics-instrumentation-plan` (DORA delivery metrics category), `feature-flag-design` (trunk-based-dev positive framing), `opentofu-module` (self-service infrastructure request template). **Agent updated**: `platform-engineer` (added `platform-engineering-design` + `kubernetes-workload-patterns` to skills:, DX review step, Operator pattern decision). 88 of 148 skills now have contract tests (up from 63 of 142). 101 files changed, 15,864 insertions.

### Changed
- 2026-07-31 — `skills/test-pyramid/SKILL.md` rebuilt to v2.0.0 under skill-authoring-standards (issue #424, PR into `issue-424-test-engineering-rebuild` integration branch): description rewritten as trigger surface naming test-strategist/implement/quality phases; Coverage Philosophy updated with formal Statement Coverage / Branch Coverage / Path Coverage hierarchy; exploratory-testing pointer added in Shift-Right section; `related:` field added (13 skills referenced in prose). Output Format template extracted to `skills/test-pyramid/references/test-strategy-template.md` (complete self-contained test-strategy document template — Pyramid Targets, Shift-Left/Right plans, Coverage/Flaky policy, Delegated Testing). Body reduced from 204 to 181 lines. New contract test: `tests/skills/test-pyramid.contract.sh` probes 70% mutation threshold from references/ file.
- 2026-07-31 — `skills/test-fixture-design/SKILL.md` rebuilt to v2.0.0 under skill-authoring-standards (issue #424, PRs into `issue-424-test-engineering-rebuild` integration branch): description rewritten as trigger surface naming test-strategist, implement phase, Test Data Builder, Object Mother, hermetic fixture, t.Cleanup, golden file, parallel-safe isolation, Testcontainers, unit-vs-integration fixture strategy; `related:` field added (go-unit-test, go-integration-test, go-e2e-test, mock-generation); all 5 Go code blocks moved from body to `skills/test-fixture-design/references/fixture-patterns-catalogue.md`. New content: Object Mother vs. Test Data Builder distinction; unit-vs-integration fixture strategy table; Khorikov maintainability rationale for builders; cross-reference to go-integration-test's rollback-vs-tenant tradeoff. `fixture-patterns-catalogue.md` (new, 420+ lines): 7 self-contained sections with full Go implementations — Test Data Builder (DataAssetBuilder), Object Mother (MakeClassifiedAsset/MakeArchivedAsset/SeedActiveTenant), t.Cleanup Teardown (SetupTestDB/FreshTenant), Deterministic Data, Golden Files (AssertGolden), Parallel-Safe Isolation, Unit vs. Integration fixture strategy decision table. New contract test: `tests/skills/test-fixture-design.contract.sh` probes SeedActiveTenant from references/ file. New canonical glossary terms warranted: Object Mother, Test Data Builder.
- 2026-07-31 — `skills/mock-generation/SKILL.md` rebuilt to v2.0.0 under skill-authoring-standards (issue #424, sub-issues #447/#450/#451/#452/#453, PRs into `issue-424-test-engineering-rebuild` integration branch): description rewritten as trigger surface naming test-strategist, implement phase, five-type taxonomy (dummy/stub/spy/mock/fake), managed vs. unmanaged dependency criterion, Classical/Detroit/Chicago school choice, interface isolation, and generation tooling (mockery/gomock/moq); `related:` field added (go-unit-test, go-integration-test, go-project-structure, test-fixture-design, react-component-testing); all Go code examples removed from body and moved to references/; three new body sections (The Core Decision: Managed vs. Unmanaged Dependencies; Classical School Not London; Generate Don't Hand-Roll); body held to 193 lines (under 200 threshold). New `references/test-double-taxonomy.md` (158 lines): complete five-type taxonomy with per-type Go code examples grounded in this repo's domain, managed vs. unmanaged dependency table with this-repo examples (fakeAssetRepo as managed, outbox publish as unmanaged), Classical vs. London school decision table with four-pillars pillar-cost rationale. New `references/go-mock-implementation.md` (263 lines): mockery `.mockery.yaml` configuration (`with-expecter: false` — direct func assignment over EXPECT() builder), generated output usage example for unmanaged EventPublisher; gomock EXPECT().Return().Times() DSL and `gomock.Controller` no-defer lifecycle (Go 1.14+); moq `//go:generate` directive; full `FakeAssetRepo` hand-written fake (`internal/test/fakes/asset_repo.go`, `sync.Mutex`, `FindByID`/`Save`/`ListByTenant`, compile-time compliance assertion `var _ domain.DataAssetRepository = (*FakeAssetRepo)(nil)`); CI freshness check (Makefile `check-generate` and GitHub Actions step). New contract test: `tests/skills/mock-generation.contract.sh` probes `with-expecter: false` — a value that exists only in the reference file (SKILL.md body names mockery as the default tool but contains no YAML configuration details), proving progressive-disclosure split is functional. Completes the three-skill test-engineering rebuild campaign under parent issue #424 (test-pyramid Chunk 49, test-fixture-design Chunk 50, mock-generation Chunk 51). New canonical glossary terms warranted: Test Double, Managed Dependency, Unmanaged Dependency.

### Added
- 2026-07-20 — `platform/` skills (12), `observability/` stack skills (3 — completes the 7-skill domain), and `platform-engineer` agent (Chunk 15)
- 2026-07-20 — `data-analytics/` skills (7) and `data-engineer` agent (Chunk 16) — completes the 13-agent roster defined in `.claude-plugin/plugin.json`
- 2026-07-20 — `validation/` skills (5) (Chunk 17); `requirements-analyst` extended with Customer Validation phase ownership (UAT, beta programs, feedback triage, acceptance sign-off) — the SDLC's final phase, run post-Deploy
- 2026-07-20 — remaining `governance/` skills (4): `artifact-manifest`, `risk-register`, `value-stream-map`, `sdlc-config-management` (Chunk 18) — completes the governance domain (6 skills) and all 15 skill domains
- 2026-07-20 — all 15 real slash commands (`commands/*.md`) — phase drivers, navigation, cross-cutting (Chunk 19). Live-verified via `claude --plugin-dir` + one real command invocation, not just read for coherence.
- 2026-07-20 — `hooks/hooks.json` with 7 real hooks and 6 backing `scripts/*.sh` (Chunk 20). The 9 originally planned hooks became 7: `pre-phase-advance`, `pre-code-generation`, and `pre-implement` consolidated into one `pre-phase-advance` hook (`PreToolUse`/`Agent`) once bound to a real event — they were the same check under three names. `methodology-compliance-check` is an agent-type handler (judgment, not scriptable); the other 6 are deterministic command-type scripts. Every hook and script individually triggered with real or synthetic input and its outcome verified.
- 2026-07-20 — `schemas/sdlc-config.schema.json` and `schemas/sdlc-manifest.schema.json` (Draft 2020-12; Chunk 21) — the final build_checklist item besides the deferred ai-ml-architect agent. Formalize `sdlc-config-management` and `artifact-manifest`'s already-documented Output Format shapes exactly, rather than inventing a new one. `sdlc-manifest.schema.json` validates only the per-product manifest instance — the master artifact-type catalog stays a prose table (extended over time, so `type` is a free-form pattern, not a closed enum). Both schemas' own worked examples validated cleanly against the real `jsonschema` library, and 15 deliberately invalid instances across both were correctly rejected (missing required fields, bad enum values, malformed dates/semver/slugs, unknown fields, empty traceability refs).
- 2026-07-22 — `skills/ddd-agent-handoff/SKILL.md` (Chunk 24, PR #51/#52) — cross-cutting Agent Boundary Matrix mapping DDD concerns to the 11 real agents that touch DDD work, pointing each to the existing pattern skill(s) it should load rather than duplicating them, plus a short Handoff Protocol. Wired into all 11 agents' `skills:` lists; verified via `tests/skills/ddd-agent-handoff.contract.sh`. Replaces an originally proposed 14-role "DDD orchestrator" with its own duplicated reference tree and embedded test suite — see `sdlc-context.json` decision D020 and GitHub issue #48 for the full analysis.
- 2026-07-22 — `skills/subdomain-distillation/SKILL.md` (new, MVP scope, Chunk 25, PR #61) — Core/Supporting/Generic Subdomain classification per Eric Evans' *Domain-Driven Design* Part IV, owned by `domain-modeler`. Pulled into the ddd-agent-handoff refactor plan rather than left as a deferred idea, since `handoff-protocol.md`'s Mode Selection Criteria depends on it.
- 2026-07-22 — `skills/miro-board-notation/SKILL.md` (new, MVP scope, Chunk 26, PR #76) — cross-cutting, owner `factory-governance`: the 5 real Miro element types (sticky note, shape, frame, connector, text) and a standard two-table Board Spec Format any skill can adopt. No live Miro API/MCP connection exists in this environment (confirmed via `claude mcp list`) — see decision D022. Pulled into the subdomain-distillation refactor plan since 3 dependent files need it, same pattern as `subdomain-distillation` itself was pulled into the ddd-agent-handoff plan.
- 2026-07-22 — `skills/microfrontend-architecture/SKILL.md` and `skills/css-styling-strategy/SKILL.md` (new, Chunk 27, GitHub issue #89, PRs #99/#100) — this plugin's frontend adopts a shell + independently-deployable-remotes microfrontend architecture, one remote per Bounded-Context-aligned fragment composed via Module Federation over Vite. Adopted as a deliberate decision anticipating multiple future products and teams (the organizational precondition every source book in `research/micro-frontends/` gates adoption on) — see decision D023 for the full reasoning, including a tension Claude flagged and Shafi resolved before proceeding. CSS Modules chosen as the isolation mechanism over Shadow DOM, since fragments are same-framework React via Module Federation, not Web Components.
- 2026-07-23 — Exploratory Testing Component added to `skills/uat-plan/SKILL.md` (Chunk 29, GitHub issue #110, PR #116) per Elisabeth Hendrickson's *Explore It!* and Crispin & Gregory's *Agile Testing* — this repo had zero exploratory testing coverage anywhere before this. Time-boxed charter template, session record shape, generative heuristics (CRUD, Boundaries, Position/Goldilocks, Interruptions, Configuration variance, Saboteur), the SFDIPOT coverage audit, and an explicit note that a human executor does the actual exploring, not the agent.
- 2026-07-23 — `research/security/secure-by-design.md` (Chunk 30, GitHub issue #120) — first file in a new 7th research cluster (`research/security/`), covering Bergh Johnsson/Deogun/Sawano's *Secure by Design*: Domain Primitives, Assertions/totality, pushing security into the Ubiquitous Language, Aggregates as trust boundaries, and security-sensitivity as an axis orthogonal to Core/Supporting/Generic subdomain classification. Checked against `access-control-model` (primary target) and noted findings for `security-architecture`, `secrets-management`, `privacy-design`, and `zero-trust-design` without necessarily fixing them yet.
- 2026-07-23 — 22 more books researched into `research/` across 5 clusters (Chunk on `research/2026-07-23-book-batch`, PR #129, plus a follow-on 6-book addition to `research/software-architecture/`): new clusters `data-governance-and-mdm` (7 books), `ai-agent-engineering` (2, meta — targets this repo's own architecture), `ai-ml-engineering` (5), `human-ai-collaboration` (2); `software-architecture` extended with Building Microservices, Clean Architecture, The Pragmatic Programmer, and Go/Node.js/Python backend books (the latter two proposing Node.js and Python as sanctioned alternate backend stacks alongside Go, per-product override — not yet applied to `CLAUDE.md`). Every file proposes candidate new skills/agents in its own section; none have been built yet except `skill-authoring-standards` (below), which the `ai-agent-engineering` cluster directly seeded.
- 2026-07-23 — `skills/skill-authoring-standards/SKILL.md` (new, v1.0.0, Chunk 32, GitHub issue #128, sub-issue #130, PR #137) — cross-cutting skill (`phase: cross-cutting`, `owner: factory-governance`, matching `glossary-management`/`methodology-review`) per Elbert Gale's *Mastering Claude Code Agent Skills* (`research/ai-agent-engineering/mastering-claude-code-agent-skills-gale.md`): the three-tier progressive disclosure model, description-as-discovery-surface discipline, a 200-line resident-content split threshold, a two-surface versioning rule, an optional `related:` composability field, and — new ground this repo had never used — a formalized `scripts/` (deterministic artifact generation/validation) and `assets/` (copyable templates) convention alongside `references/`. `CLAUDE.md` gained a matching "Skill Authoring & Progressive Disclosure" section (sub-issue #131, PR #138), reconciling the new skill-owned `scripts/` context against the pre-existing repo-root `scripts/` convention. See decision D028.
- 2026-07-24 — 16 books researched into `research/` across 6 clusters (Chunk 40, branch `research/2026-07-24-book-batch-2`, two sessions on the same open PR), requested directly by Shafi. 2 of 18 originally-named titles (*API Design Patterns* — Geewax, *User Story Mapping* — Patton) were already covered from recent sessions and skipped by explicit choice. All 16 executed via parallel spawned research agents, each independently verified by direct read rather than trusted from self-reports. New clusters: `product-strategy-and-gtm` (7 books — Moore's *Crossing the Chasm*, Dunford's *Obviously Awesome*, Olsen's *The Lean Product Playbook*, Lauchengco's *Loved*, Cagan's *Inspired*, Fitzpatrick's *The Mom Test*, Lemay's *Product Management in Practice*; all cross-reference each other's overlap explicitly). **Most consequential single finding**: Cagan's *Inspired*, applied honestly to this repo's own architecture, finds a real structural mismatch — the phase-gated pipeline sequentially hands a fully-decided backlog to engineering only after requirements and architecture are already locked, precisely the "engineers excluded from discovery" pattern Cagan names as a feature-factory cause; diagnostic, not a call to restructure, since no literal team exists to reorganize. `storytelling-and-narrative` (2 books — Luhn's *The Best Story Wins*, Dietz & Silverman's *Business Storytelling For Dummies*; both author attributions verified via web search rather than assumed). `service-design` (1 book — Stickdorn et al.'s *This Is Service Design Doing*, confirming the service blueprint's front-stage/back-stage Line of Visibility is a genuinely missing artifact type). `testing`, `software-architecture`, and `requirements-and-user-stories` clusters extended with Cruz & Prescott's *Contract Testing in Action*, Winteringham's *Testing Web APIs*, Kin Lane's *The API-First Transformation*, Clements et al.'s *Documenting Software Architectures* (found a genuine conflation in `container-diagram`, mixing component-and-connector and allocation concerns in one unexamined diagram), Fairbanks' *Just Enough Software Architecture* (found `adr-authoring`'s ADR gate is qualitative-only with no quantified risk filter, connecting directly to `CLAUDE.md`'s own frugality principle; correctly detected and resolved a mid-task race condition when the Clements sibling file appeared on disk mid-write), and a low-confidence entry for *Learn Product Management with Stories* (honestly flagged, later sharpened rather than merely confirmed by Lemay's higher-confidence book). 6 candidate new skills surfaced (`positioning-canvas`, `kano-prioritization`, `sales-trial-enablement-content`, `service-blueprinting`, `discovery-prototyping`, `customer-discovery-interviewing`), none built yet — consistent with every prior book-batch chunk's practice of deferring implementation to a later, dedicated session.

### Changed
- 2026-07-22 — `skills/subdomain-distillation/SKILL.md` refactored from MVP (81 lines, Evans-only) to a thin (120-line) progressive-disclosure entry point, v2.0.0 (Chunk 26, GitHub issue #68, PRs #77/#78/#79/#80/#81/#82) — checked against five additional named sources beyond Evans: Vaughn Vernon's *Domain-Driven Design Distilled* (outsource-test heuristic, one-or-two-Core-Domains warning), Vlad Khononov's *Learning Domain-Driven Design* (complexity-vs-strategic-importance axes, flagging a real tension with this repo's own blanket DDD-tactical-patterns rule in `CLAUDE.md`), Susanne Kaiser's *Architecture for Flow* (Wardley Mapping evolution axis), Dan Bergh Johnsson's *Secure by Design* (a security-sensitivity axis separate from Core/Supporting/Generic, correcting the MVP's Authentication/Authorization example), and Carola Lilienthal & Henning Schwentner's *Domain-Driven Transformation* (legacy transformation sequencing, Strangler Fig, Domain Vision Board). Added `references/classification-techniques.md`, `references/security-sensitive-subdomains.md`, `references/legacy-transformation-guidance.md`, and `assets/subdomain-classification-canvas.md`. `tests/skills/subdomain-distillation.contract.sh` updated to exercise the new deep content and live-verified to follow the progressive-disclosure pointer.
- 2026-07-22 — `skills/ddd-agent-handoff/SKILL.md` refactored from a single 71-line file to a thin (98-line) progressive-disclosure entry point, v2.0.0 (Chunk 25, GitHub issue #54, PRs #62/#64/#65/#66/#67) — the first skill processed under the skill-refactor campaign. Added `references/handoff-protocol.md` (handoff record schema, the 3 Team Topologies interaction modes, Mode Selection Criteria, Definition of Done per stage, Mode Transition and Roster Change protocols, conflict resolution, a Pass/Defect checklist), `references/handoff-worked-examples.md` (7 worked examples), and `assets/handoff-record-template.md` (fill-in template with mode-conditional completion rules). Checked directly against Eric Evans' *Domain-Driven Design* — see decision D021 — which surfaced two real gaps now folded in: Core/Supporting/Generic Subdomain classification as the primary mode-selection input, and an operational-pause vs. breakthrough-triggered-refactor distinction for resumed sessions. `tests/skills/ddd-agent-handoff.contract.sh` updated to exercise the new deep content and live-verified to actually follow the progressive-disclosure pointer, not just the thin `SKILL.md` surface.
- 2026-07-22 — `scripts/github-project/plan_status.py` (PR #53) — marking a plan `done` now clears `current_plan` from local state, which nothing previously did; this was blocking `plan-start` for every subsequent skill refactor.
- 2026-07-22 — `scripts/github-project/exec_complete.py` (PR #63) — every commit now gets a deterministic `git diff --staged --stat` block appended to its body, so commits are self-documenting regardless of the hand-typed subject line.
- 2026-07-22 — `skills/react-project-structure/SKILL.md`, `skills/react-routing/SKILL.md`, `skills/react-state-management/SKILL.md`, `skills/ui-component-spec/SKILL.md` all rewritten for the shell + remotes microfrontend layout (v2.0.0 each, Chunk 27, GitHub issue #89, PRs #101/#102/#103/#104) and split into progressive disclosure. Notable content: `react-state-management` now answers the cross-fragment state question the single-app version was silent on (each fragment owns its own TanStack Query cache and any client-state store, never shared — only the shell context and custom events cross a boundary); `react-routing` adds remote-load failure as a distinct error-handling category from an ordinary route render error; `ui-component-spec` adds a Shared (federated design-system) vs. Local (one fragment) component dimension with contract versioning for Shared components.
- 2026-07-22 — `agents/frontend-engineer.md` and `agents/ux-architect.md` updated (v2.0.0 each, Chunk 27, PRs #105/#106) for the microfrontend architecture — new skills wired in, fragment-boundary and singleton-discipline directives, Shared/Local component scope declared as a joint call between the two agents. `agents/platform-engineer.md` (per-fragment CI/CD) and `agents/enterprise-architect.md` (updated topology diagrams) are explicitly flagged as real follow-ups, not yet in scope.
- 2026-07-22 — Ended the plan-start/plan-exit GitHub Project ceremony for future individual skill refactors (decision D024) — skill refactors continue directly (read, gap-check, progressive-disclosure split, rewrite, commit) without a new parent issue + sub-issue set per skill, given the repeatable process established across two prior full cycles (`ddd-agent-handoff`, `subdomain-distillation`).
- 2026-07-23 — `skills/acceptance-criteria/SKILL.md` (v2.0.0, Chunk 28) — the first skill refactored under the no-ceremony process (D024), no GitHub issue for this one. Added a "Deriving Criteria from a Worked Example" section per Ken Pugh's Acceptance Test-Driven Development (ATDD) practice (`research/requirements-and-user-stories/lean-agile-atdd-pugh.md`): criteria are derived from a concrete worked example via `example-mapping` first, never decided as an abstract rule and illustrated afterward. Split into progressive disclosure: `references/output-format-template.md`. Fixed a real sequencing bug the research surfaced: `agents/requirements-analyst.md`'s Execution Sequence ran Acceptance Criteria before Example Maps, backwards from Specification-by-Example's examples-first order — swapped, with `skills/example-mapping/SKILL.md` (v1.2.0) corrected to match (its own step 2 previously required acceptance criteria to already exist, which no longer holds). Added "Acceptance Test-Driven Development (ATDD)" and "Three Amigos" to the canonical glossary (`skills/glossary-management/references/ubiquitous-language.md`), with the existing "Acceptance Testing" entry updated to cross-reference and distinguish the two — preempting a terminology collision. Whether ATDD should become a 6th non-negotiable methodology in `CLAUDE.md` is recorded as an open question (`sdlc-context.json` Q002), not decided unilaterally.
- 2026-07-23 — `skills/acceptance-sign-off/SKILL.md` (v2.0.0, Chunk 29, GitHub issue #110, PR #118) updated to check exploratory-session findings alongside scripted UAT results in its Sign-Off Criteria Checklist — absorbed through the existing `feedback-template` triage/blocking-pattern mechanics with no structural rewrite, confirming the direct research finding (`research/testing/agile-testing-crispin-gregory.md`) that this skill itself had no gap, only its upstream input (`uat-plan`) did. Split into progressive disclosure. `agents/requirements-analyst.md` (v1.3.0, PR #117) gains an explicit Exploratory Charters step in its Customer Validation Execution Sequence and a distinct completion criterion; both `requirements-analyst.md` and `agents/test-strategist.md` (v1.2.0) gain an identical note mapping the Agile Testing Quadrants across the two agents (Q1/Q2/Q4 vs. Q3), previously implicit and undocumented.
- 2026-07-23 — `scripts/github-project/exec_start.py` (PR #109) gains an optional `--base <branch>` flag, letting a sub-issue's branch/PR target something other than the default branch — enables an issue-level integration-branch pattern (one parent issue, sub-issue PRs merging into an integration branch in sequence, a single final PR for human review) used for the first time in Chunk 29 (decision D025), reintroducing GitHub issue tracking for this specific case per Shafi's request, distinct from D024's default no-ceremony process for ordinary skill refactors.
- 2026-07-23 — `skills/access-control-model/SKILL.md` (v2.0.0, Chunk 30, GitHub issue #120, PR #126) refactored per `research/security/secure-by-design.md`: new "Attributes Are Domain Primitives, Not Bare Types" section (`TenantID`/`Permission`/`Sensitivity` as self-validating constructors, cross-referencing `subdomain-distillation`'s existing `references/security-sensitive-subdomains.md` rather than re-explaining the general concept); Go Policy Enforcement Pattern condensed with a pointer to the full before/after code; new Per-Aggregate Trust-Boundary Checklist section; Multi-Tenancy section clarified so the physical-multi-tenancy defence-in-depth argument does not excuse skipping Domain Primitives; two new Quality Criteria rows and two new Anti-Patterns (primitive obsession, discarded constructor errors). Split into progressive disclosure: `references/domain-primitives-and-enforcement.md`, `references/output-format-template.md`. `tests/skills/access-control-model.contract.sh` added, live-verified against a detail (the Go zero-value trap) that only exists in the reference file. Second use of the issue-level integration-branch pattern (issue #120, sub-issues #122-#124, integration PR #121) — this time with decision D026 correcting a process gap from Chunk 29: `plan_status.py done` is deferred until Shafi's actual merge of the final PR, not called as soon as sub-issue work lands on the integration branch.
- 2026-07-23 — `skills/adr-authoring/SKILL.md` (v2.0.0, Chunk 31, no GitHub issue — D024 no-ceremony process) refactored per Richards & Ford's *Fundamentals of Software Architecture* Ch. 4/18 (`research/software-architecture/fundamentals-of-software-architecture-richards-ford.md`), the only research source naming `adr-authoring` as a primary target. New "Architecture Decisions vs. Architecture Principles" section and a companion Architecture Principle artifact type (decision D027 — durable, cross-cutting guidance many ADRs cite, distinct from a point-in-time decision); ADR Format's Rationale now requires a `**Trade-off:**` line naming the 2-3 competing architecture characteristics explicitly, with an optional trade-off matrix in Options Considered for 3+ competing characteristics. Split into progressive disclosure: `references/architecture-principles.md` (distinction, Principle format template, storage convention, one illustrative worked example built on `integration-design`'s existing async-by-default prose rule), `references/output-format-template.md` (ADR template with both new requirements inline), `references/worked-example.md` (the original Transactional Outbox ADR plus a new Mediator-vs-Broker-topology ADR demonstrating the trade-off matrix). `agents/enterprise-architect.md` (v1.2.0) gains a Principle Catalog output and a one-sentence pointer to the decision-vs-principle test. `skills/glossary-management/references/ubiquitous-language.md` gains a new "Architecture Decisions" section (4 terms, including ADR itself, never formally defined before). `tests/skills/adr-authoring.contract.sh` added, live-verified against a detail (Principles are revised in place, not superseded) that only exists in the reference file. Whether to retroactively formalize this repo's own existing conventions as Principle records is recorded as an open question (`sdlc-context.json` Q003), not decided unilaterally.
- 2026-07-23 — `skills/adr-authoring/SKILL.md` rebuilt (v3.0.0, superseding v2.0.0 — never edited in place, per this skill's own ADR-supersession philosophy; Chunk 32, GitHub issue #128, sub-issues #132-#136, PRs #140-#144) as the flagship pilot of `skill-authoring-standards` (above), re-derived against Clean Architecture, The Pragmatic Programmer, and Building Microservices alongside the existing Richards & Ford foundation. Honest finding: none of the three new books surfaced a direct content gap in the ADR methodology itself — the v3.0.0 value-add is the skill's *shape*, not manufactured new prose; core ADR Format, Status Lifecycle, Numbering, Storage, Quality Criteria, and Anti-Patterns content is preserved. New `assets/adr-template.md` (literal, copyable template) and `scripts/scaffold-adr.sh` (auto-resolves the next `ADR-NNN`, guarding a real bash base-10 arithmetic pitfall for numbers like 008/009) and `scripts/validate-adr.sh` (mechanically checks required frontmatter, the Rationale `**Trade-off:**` line, all three Consequences subsections, and a Related ADRs section — deliberately structure-only, leaving judgment calls to the enterprise-architect's own review). `tests/scripts/scaffold-adr.test.sh` and `tests/scripts/validate-adr.test.sh` added (synthetic input, no LLM call) — writing them caught two real bugs before landing: a scaffold call writing into the actual repo tree instead of the test scratch dir, and an exit-code capture reading a negated `if !` condition's status instead of the script's own. `tests/skills/adr-authoring.contract.sh`'s question changed to test v3.0.0-specific content. See decision D028.
- 2026-07-24 — `skills/acceptance-criteria/SKILL.md` rebuilt (v2.1.0, Chunk 33, GitHub issue #145, sub-issues #146-#149, PRs #150-#154) as the second skill to adopt the `skill-authoring-standards` four-part shape. Checked against all 6 `research/` files mentioning acceptance criteria (Pugh, Cohn, Hathaway, Lee, Hendrickson, Crispin & Gregory) before writing anything — honest finding, consistent across all six: no new content gap. The only real addition is a one-line Three Cs "Confirmation" pedigree note (Ron Jeffries); the actual value of this bump is shape, not manufactured prose. New `assets/acceptance-criteria-template.md`, `scripts/scaffold-criteria.sh` (generates a new criteria doc pre-filled with story metadata — caught and fixed a real bug where a naive placeholder replace also overwrote the unrelated `EM-[ID]` example-map field, silently inventing a fake traceability link), and `scripts/validate-criteria.sh` (checks testability red-flag words, per-section Golden Triangle coverage, and a non-placeholder Derived From field). `tests/scripts/scaffold-criteria.test.sh` and `tests/scripts/validate-criteria.test.sh` added (12 assertions total, no LLM call). `tests/skills/acceptance-criteria.contract.sh` updated to test v2.1.0-specific content, live-verified.
- 2026-07-24 — `skills/acceptance-sign-off/SKILL.md` rebuilt (v2.1.0, Chunk 34, GitHub issue #155, sub-issues #156-#159, PRs #160-#164) as the third skill to adopt the `skill-authoring-standards` shape. Checked against every `research/` file mentioning sign-off — Crispin & Gregory's is the one substantive hit, and its own conclusion is "nothing structural to add." The one real addition: naming the Sign-Off Authority's two-party, never-unilateral rule as this repo's clearest product-facing instance of Mollick's human-in-the-loop principle (`research/human-ai-collaboration/co-intelligence-mollick.md`) — the same principle `CLAUDE.md`'s own Session Startup gate already applies to this repo's own engineering work. New `assets/sign-off-template.md` (full 8-item checklist copied in unchecked), `scripts/scaffold-signoff.sh` (generates a pre-filled sign-off doc — solved a precision problem where the template's `name:` field and heading share one placeholder token but need different substitutions, a slug vs. human-readable text), and `scripts/validate-signoff.sh` (checks the checklist, both named parties, a valid Decision outcome, and a stated Rollout Action). `tests/scripts/scaffold-signoff.test.sh` and `tests/scripts/validate-signoff.test.sh` added (13 assertions, no LLM call). `tests/skills/acceptance-sign-off.contract.sh` updated to test v2.1.0-specific content, live-verified.
- 2026-07-24 — `skills/access-control-model/SKILL.md` rebuilt (v2.1.0, Chunk 35, GitHub issue #165, sub-issues #166-#169, PRs #170-#174) as the fourth and last skill in this batch to adopt the `skill-authoring-standards` shape. Checked against every `research/` file mentioning access control — DAMA-DMBOK's own file explicitly declines to propose a security skill since `Secure by Design`'s coverage (already synthesized here) is thorough; Pragmatic Programmer's Design by Contract note is conditional, not a gap. No new content added — this skill's methodology was already complete. New `assets/access-control-model-template.md` and `scripts/scaffold-access-control-model.sh` (generates a pre-filled design doc — deliberately does not generate Go source, since this skill's own artifact is the markdown design document, not implementation code) and `scripts/validate-access-control-model.sh` (checks required frontmatter and that the Domain Primitives/Policies/Trust-Boundary tables have real data rows, not just headers — catching a design doc with the right structure but empty content). `tests/scripts/scaffold-access-control-model.test.sh` and `tests/scripts/validate-access-control-model.test.sh` added (11 assertions, no LLM call). `tests/skills/access-control-model.contract.sh` updated to test v2.1.0-specific content, live-verified. This completes the three-skill refactor batch (`acceptance-criteria`, `acceptance-sign-off`, `access-control-model`) requested alongside the `adr-authoring` pilot (#128) — all four skills' research checks independently confirmed the same pattern: the value of adopting `skill-authoring-standards` is shape, not new methodology.
- 2026-07-24 — `skills/aggregate-design/SKILL.md` extensively rebuilt (v1.1.0 → v2.0.0, MAJOR; Chunk 36, GitHub issue #175, 11 sub-issues #176-#186, PRs #187-#197) — the deepest single-skill rebuild attempted in this repo. New research cluster `research/domain-driven-design/` (Evans' *Domain-Driven Design*, Vernon's *Implementing Domain-Driven Design*/"Effective Aggregate Design", Khononov's *Learning Domain-Driven Design* — see decision D029), researched as a genuine prerequisite since no dedicated DDD-Aggregate research existed despite the skill clearly summarizing these exact sources. **Most consequential finding**: the skill's prior header ("these rules are non-negotiable") was materially more absolutist than Vernon's own explicitly-not-absolutist position — corrected, with a documented, criteria-gated exception process. New content: Khononov's Business Logic Design Pattern gate (Transaction Script/Active Record/Domain Model/Event-Sourced Domain Model) as the first decision a modeler makes; full event-sourced Aggregate Go patterns (`Apply`/`When` methods, conditional-append repository, snapshotting, event upcasting) — the first event-sourced pattern this repo has ever had; Factory and Specification patterns (previously entirely absent); the Value-Object-is-Domain-Primitive equivalence tying directly to `access-control-model`'s existing pattern. Split into 7 `references/` files (2,170 total lines: `invariants-and-consistency-boundaries.md`, `sizing-contention-and-concurrency.md`, `cross-aggregate-coordination-and-sagas.md`, `entities-value-objects-and-domain-primitives.md`, `go-implementation-patterns.md` at 777 lines, `worked-examples.md` with 4 full designs, `decision-trees-and-worksheets.md` with 6 explicit branching decision trees — the literal "sentinel"-style decision support Shafi asked for by name). 18 new terms added to the canonical glossary. New `assets/aggregate-design-template.md`, `scripts/scaffold-aggregate-design.sh`, `scripts/validate-aggregate-design.sh` (includes a Go-pointer-reference smell check). 12 script-test assertions, no LLM call; `tests/skills/aggregate-design.contract.sh` updated to test a detail that exists only in `references/` (verified absent from `SKILL.md` via grep), proving the progressive-disclosure split is genuinely followed. Executed via the issue-level integration-branch pattern (D025), with all research and content-drafting sub-issues run through spawned foreground agents to keep the driving session's context focused on orchestration. Per Shafi's explicit instruction, the final integration-to-main PR (unlike the prior three-skill batch) is left open in review-ready state, not auto-merged.
- 2026-07-24 — `skills/alerting-rules-design/SKILL.md` rebuilt (v1.0.0 → v2.0.1; Chunk 37, GitHub issue #199, sub-issues #200-#209, PRs #202-#215) — reorganized around Google's Four Golden Signals (Latency, Traffic, Errors, Saturation; see decision D030) per new research file `research/devops-and-delivery/site-reliability-engineering-beyer-jones-petoff-murphy.md`. **Key finding**: the skill's existing 14.4x/6x/1x burn-rate table was mathematically correct but uncited and underivable — it is a 2018 *SRE Workbook* artifact the skill's own 2016 SRE-book framing never named; now correctly two-source-cited, with the full derivation arithmetic (`0.02 × 672 = 13.44 ≈ 14.4`) in a new reference file. New content: a generic, resource-agnostic Saturation alert pattern (`deriv()`-based trend projection, promoting `prometheus-metrics-design`'s existing `db_pool_in_use / db_pool_max` dashboard query to an actual ticket-severity alert) — the fourth golden signal had no prior generic treatment beyond pipeline-specific DLQ/lag alerts; a toil check and pager-load heuristic in the monthly review, honestly reframed as a personal-sustainability signal rather than Google's team-scale 50%-ceiling policy; an honestly-scoped solo-operator escalation section (`repeat_interval` is explicitly not an escalation policy) and a postmortem-linkage review check (postmortem authoring itself stays out of scope, noted as a future candidate). Split into 3 `references/` files: `burn-rate-derivation-and-golden-signals.md`, `toil-and-review-discipline.md`, `escalation-and-postmortem-linkage.md`. 9 new terms added to the canonical glossary. New `assets/alerting-rules-design-template.md`, `scripts/scaffold-alerting-rules-design.sh`, `scripts/validate-alerting-rules-design.sh` — writing the test suite surfaced and fixed a real bug: the Output Format's markdown tables were missing `|---|` separator rows, unlike every sibling rebuilt skill's template (PATCH bump within this chunk, v2.0.0 → v2.0.1). 13 script-test assertions, no LLM call; `tests/skills/alerting-rules-design.contract.sh` created new (none existed before) testing a detail that exists only in `references/` (the `13.44` pre-rounding arithmetic, verified absent from `SKILL.md` via grep — the originally-suggested SRE Workbook citation example was checked and rejected first since `SKILL.md` already names it directly). Executed via the issue-level integration-branch pattern (D025); this session resumed the effort mid-flight (discovery and the architecture blueprint had already landed) and completed the two content sub-issues, scripts/assets, tests, and this housekeeping sub-issue in sequence. Per Shafi's default, the final integration-to-main PR is left open for his own review, not auto-merged.
- 2026-07-24 — `skills/analytics-requirements/SKILL.md` rebuilt (v1.0.0 → v1.1.0; Chunk 38, GitHub issue #217, sub-issues #218-#225, PRs #220-#229) — a modest, shape-plus-small-content rebuild per new research file `research/data-and-analytics/lean-analytics-croll-yoskovitz.md` (Croll & Yoskovitz's *Lean Analytics*, 2013), matching the `acceptance-criteria`/`access-control-model` precedent rather than the deeper multi-file `aggregate-design`/`alerting-rules-design` precedent — no separate architecture-blueprint sub-issue was drafted. **Key finding**: the skill's five-point vanity-metric checklist was already, in substance, Croll & Yoskovitz's own actionable-vs-vanity-metric framework (three of the book's four tests map near-verbatim) but cited no source anywhere — an attribution gap, not a correctness bug; now cited, with a sixth "Understandable" criterion added and a cohort-analysis technique added for retention/stickiness-flavored metrics specifically (a rising cumulative count can pass every other check while still hiding declining per-cohort retention). A clarifying note distinguishes the book's One Metric That Matters (stage-relative, deliberately rotating) from this repo's North Star Metric (fixed per OKR cycle) — see decision D031; OMTM was deliberately *not* added to the canonical glossary, per the research file's own caution that doing so on the strength of one skill's cross-reference would be premature. Split into 1 `references/` file (`output-format-template.md`, the annotated template, mirroring `acceptance-criteria`'s exact convention). New `assets/analytics-requirements-template.md`, `scripts/scaffold-analytics-requirements.sh`, `scripts/validate-analytics-requirements.sh`. 13 script-test assertions, no LLM call; `tests/skills/analytics-requirements.contract.sh` created new (none existed before). 1 new glossary term (`Cohort Analysis`). Executed via the issue-level integration-branch pattern (D025); sub-issue #219 (confirm-existing-coverage, zero file changes) was completed manually since `exec_complete.py` requires staged changes — matching the precedent set by `alerting-rules-design`'s equivalent sub-issue #201. Per Shafi's default, the final integration-to-main PR is left open for his own review, not auto-merged.
- 2026-07-24 — `skills/api-contract-design/SKILL.md` rebuilt (v1.1.0 → v2.1.0; Chunk 39, GitHub issue #231, sub-issues #232-#241, PRs #234-#247, plus a v2.1.0 addendum) — a genuine multi-file rebuild per new research file `research/software-architecture/api-design-patterns-geewax.md` (JJ Geewax's *API Design Patterns*, 2021), executed with both research and content-writing sub-issues run through spawned foreground agents per explicit instruction that this session's discovery work be done properly, not shortcut. **Key finding**: this skill — the authoritative contract between `backend-engineer` and `frontend-engineer` — had zero primary-source grounding anywhere before this rebuild, despite being one of the most heavily-referenced skills in the repo. Four genuine content gaps closed (decision D032): a named **custom-method** escape hatch (`POST /{resource}:verb`) alongside the existing "Verb endpoints" anti-pattern, for the rare action that genuinely isn't a resource-field update; a fully-specified **`Operation` resource** shape for the `202 Accepted` status-check response (previously unspecified — the skill said a client polls "a status URL" but never what it returns), reusing the skill's existing `ErrorResponse.error` object by reference rather than redefining a new error type; a **resource-revisions** (`etag`/`If-Match`) optimistic-concurrency mechanism, distinct from the existing `409 Conflict` business-rule row; and a **field-mask** convention clarifying `PATCH` partial-update semantics. Explicitly declined to change the skill's per-field-query-parameter filtering to match the book's filter-expression-string approach — confirmed as a deliberate, frugality-justified choice, not an oversight. Split into 3 `references/` files: `advanced-resource-patterns.md` (the four new patterns, with worked YAML examples grounded in this repo's own domain — `EstateScan`, `ComplianceGap`, `DataAsset`), `openapi-spec-structure-example.md` (the full worked OpenAPI YAML, moved out of `SKILL.md` and extended with the new `Operation` schema), `output-format-template.md` (the annotated `api-contract-summary.md` template). New `assets/api-contract-summary-template.md`, `scripts/scaffold-api-contract-design.sh`, `scripts/validate-api-contract-design.sh` — the validator explicitly distinguishes a filled Endpoints row from the template's own unfilled placeholder row. 13 script-test assertions, no LLM call; `tests/skills/api-contract-design.contract.sh` created new (none existed before). 5 new glossary terms. Every cross-file consistency claim (the `Operation` schema, `ErrorResponse` schema, and `Idempotency-Key` parameter matching identically between `SKILL.md` and the two reference files) was independently re-verified via direct diff, not trusted from the spawned agents' self-reports. Executed via the issue-level integration-branch pattern (D025); sub-issue #233 (confirm-existing-coverage, zero file changes) was completed manually, matching the precedent set by sub-issues #201 and #219. **v2.1.0 addendum** (MINOR, added before merge): a later, separate 16-book research pass found Kin Lane's *The API-First Transformation* had 3 more small, genuinely new findings — checked all 16 of that batch's files against `api-contract-design` first; only Lane's had anything actionable. Added a "contract is a product, not plumbing" framing sentence to the Purpose section; a new **API Contract Review Checklist**, a solo-operator-scale substitute for Lane's governance-board concept, built entirely from the existing Quality Criteria table's own rows; and a second illustrative Principle (`PRINCIPLE-002`, "APIs Are Durable Products, Not UI Plumbing") in `skills/adr-authoring/references/architecture-principles.md` (`adr-authoring` v3.0.0 → v3.1.0). Explicitly declined an API governance-board or catalog/marketplace skill — organizational-scale machinery with no referent here. Per Shafi's explicit instruction this session, the integration-to-main PR was merged directly, departing from the usual leave-open-for-review default.
- 2026-07-24 — `skills/artifact-manifest/SKILL.md` rebuilt (v1.0.0 → v2.0.0, MAJOR; Chunk 41, GitHub issue #251, sub-issues #252, #255-#259, PRs #254, #260-#264) — the last governance-domain skill to adopt `skill-authoring-standards` progressive disclosure (siblings `ddd-agent-handoff`, `glossary-management`, `methodology-review` already had it). **First skill-refactor effort where Wave 1 discovery found no book in the `research/` corpus grounds the target skill's topic** (sub-issue #252): DAMA-DMBOK's Metadata Management, Loshin's MDM ID/lifecycle content, Continuous Delivery's Configuration Management chapter, and Documenting Software Architectures' documentation-package structure are each already scoped by their own Applies To tables to a different skill — see decision D034. Wave 2 was therefore an evidence-based restructuring of the skill's own existing content, not a research-driven content addition: thin 131-line `SKILL.md` retaining all decision-shaping guidance (Purpose, Two Registries distinction, Artifact ID Scheme, Traceability, Status Lifecycle, hook connection, Quality Criteria, Anti-Patterns); `references/artifact-type-catalog.md` (the full 21-row master catalog table, plus a new Directory-Shaped Artifact Types subsection); `references/manifest-worked-example.md` (the original 2-entry worked example expanded to 4, adding a `draft`-status entry and a directory-shaped Go Service entry to illustrate the "one manifest entry per meaningful unit" rule); `assets/manifest-instance-template.json` (a schema-valid empty starting shape — using schema-legal placeholder values like `replace-with-product-slug` and the sentinel date `0001-01-01` instead of bracket placeholders, since `schemas/sdlc-manifest.schema.json`'s `FormatChecker`-enabled validation would reject those outright). Explicitly declined a skill-owned scaffold/validate script pair: the existing schema and `tests/schemas/sdlc-manifest.test.py` already cover validation, so a script would duplicate rather than close a gap. New `tests/skills/artifact-manifest.contract.sh` (none existed before), live-verified against a detail (the worked example's draft-status entry ID) confirmed absent from `SKILL.md` via grep. 3 new glossary terms (`Artifact Manifest`, `Artifact-Type Catalog`, `Traceability`) added under a new "Governance and Traceability" section — none of the existing 10 sections fit precisely. Executed via the issue-level integration-branch pattern (D025); this session's work was briefly paused mid-effort by a confirmed GitHub platform-wide PR-creation outage (issue creation worked, PR creation returned HTTP 500) and resumed cleanly once GitHub's own incident status confirmed it, with no lost work. Per Shafi's default, the final integration-to-main PR is left open for his own review, not auto-merged.
- 2026-07-24 — `skills/bdd-feature-file/SKILL.md` rebuilt (v1.1.0 → v2.0.0, MAJOR; Chunk 42, GitHub issue #266, sub-issues #267, #268, #271-#275, PRs #269, #270, #276-#280) — this skill's first `skill-authoring-standards` progressive-disclosure adoption, a modest content addition matching the `acceptance-criteria`/`access-control-model` precedent rather than a deep multi-file rebuild. **Two parallel Wave 1 discovery sub-issues**: #268 confirmed and consolidated four already-existing tangential research files (Ken Pugh's ATDD — a provenance note only, the skill already fully implements the declarative-vs-imperative point; Crispin & Gregory's Agile Testing — the one real gap, naming the Three Amigos collaborative-authorship expectation; Cohn and Lee — confirmed zero findings via grep; Jorgensen — confirmed tangential, routed elsewhere in its own Applies To table). #267 produced new `research/testing/specification-by-example-adzic.md` (Gojko Adzic, 3,048 words), grounding "Specification by Example" — an already-used-but-uncredited canonical glossary term. **Most consequential finding**: the skill's existing Living Documentation section was a faithful but *partial* implementation of Adzic's own definition — it captured "current because tested in CI" but never named the other half, mechanically deriving a browsable documentation artifact from the executed spec suite. New `references/collaborative-specification-and-living-documentation.md` (Specification Workshop definition-of-ready, Living Documentation's full two-part definition, the Key Examples minimality principle as a distinct, complementary axis to the existing Golden Triangle). `SKILL.md` itself gained a Three Amigos naming addition, a Pugh/Adzic provenance note, and a new Anti-Patterns row for untended specification growth. First scaffold/validate script pair in the `test-engineering` domain: `assets/bdd-feature-file-template.feature`, `scripts/scaffold-bdd-feature-file.sh`, `scripts/validate-bdd-feature-file.sh` (Golden Triangle tag presence, imperative/HTTP-mechanics leakage, multiple-`When`-per-`Scenario`) — live testing caught and fixed a real bug before landing: gawk's `\b` inside a dynamic regex means a literal backspace character, not a word-boundary assertion, so the multi-`When` check originally matched nothing regardless of input. 11 script-test assertions (no LLM call) plus a regression test naming the bug explicitly; new `tests/skills/bdd-feature-file.contract.sh`, live-verified. 5 new glossary terms (`Feature File`, `Given/When/Then`, `Living Documentation`, `Scenario`, `Specification Workshop`) — three of the five were already named in `CLAUDE.md`'s own canonical Testing terms table but had no standalone glossary headword before this rebuild. Executed via the issue-level integration-branch pattern (D025); see decision D035. Per Shafi's default, the final integration-to-main PR is left open for his own review, not auto-merged.
- 2026-07-24 — `skills/beta-program-design/SKILL.md` rebuilt (v1.0.0 → v2.0.0, MAJOR; Chunk 43, GitHub issue #282, sub-issues #283, #284, #287-#291, PRs #285, #286, #292-#296) — this skill's first `skill-authoring-standards` progressive-disclosure adoption. **Two parallel Wave 1 discovery sub-issues**: #284 confirmed and consolidated three tangential research files re-checked specifically against `beta-program-design` (Fitzpatrick's Mom Test — non-gap, the skill already delegates cleanly to `feedback-template`'s existing anti-leading-question guidance; Moore's Crossing the Chasm — one real gap, a missing "credible reference for stage-expansion" Participant Selection criterion; Hendrickson's Explore It! — non-gap, targets `uat-plan`). #283 produced new `research/product-strategy-and-gtm/four-steps-to-the-epiphany-blank.md` (Steve Blank's Customer Development methodology, 4,000 words). **Most consequential finding of the campaign to date**: `CLAUDE.md`'s own eight-phase model names its final phase "Customer Validation" — this is literally Steve Blank's Customer Development terminology (Customer Discovery → **Customer Validation** → Customer Creation → Company Building), never once researched despite being load-bearing across the entire phase model, not just this one skill. The research confirmed `beta-program-design`'s four internal stages (closed alpha/closed beta/open beta/GA) map cleanly as a rollout-mechanics subdivision entirely *within* Blank's single Customer Validation stage — a validating finding, not a defect — while also surfacing three genuine, additive content gaps: a second Participant Selection criterion (evidence of prior self-funded effort to solve the problem, Blank's earlyvangelist condition 2, distinct from Moore's credible-reference gap since one predicts motivated-pain feedback quality and the other predicts stage-expansion endorsement transfer); a third Graduation Criteria outcome (**pivot**, alongside the existing extend/remediate options, naming that a failed bar can mean the segment/problem/solution hypothesis itself needs revision); and an explicit "outside the building" check before a qualitative graduation bar is marked met. New `references/customer-development-and-graduation-discipline.md` (the four-condition earlyvangelist screening checklist, the pivot decision framework, the outside-the-building check, and a repeatability check for `acceptance-sign-off` evidence distinct from per-participant usability affirmation). `SKILL.md` itself gained a Purpose-section provenance note, two new Participant Selection rows, and a three-outcome Graduation Criteria table. New `assets/beta-program-design-template.md`, `scripts/scaffold-beta-program-design.sh`, `scripts/validate-beta-program-design.sh` — five checks split deliberately between two placeholder-sensitive checks (Stage, Participants) and three purely-structural presence checks (SOC 2 CC6/CC7/A1 codes, both Graduation Criteria dimensions), matching `acceptance-criteria`'s established precedent of not treating every check as fill-detection. 13 script-test assertions (no LLM call); new `tests/skills/beta-program-design.contract.sh` caught and fixed a real case-sensitivity bug in the test itself (not the skill) before landing. 5 new glossary terms (`Customer Development`, `Customer Validation`, `Earlyvangelist`, `Get Out of the Building`, `Pivot`), including a bidirectional disambiguation between the new `Customer Validation` entry and the existing `Product Validation` entry — Blank's research file explicitly flagged the two as a near-duplicate collision risk if added carelessly, since `Product Validation`'s existing definition actually describes Blank's Customer *Discovery*, not Customer *Validation*. Executed via the issue-level integration-branch pattern (D025); see decision D036. Per Shafi's default, the final integration-to-main PR is left open for his own review, not auto-merged.
- 2026-07-25 — All 13 `react-*` `frontend-engineering` skills rebuilt as one grouped, autonomous campaign (Chunk 44 — provisional numbering, may be renumbered 45 at merge time depending on which of this PR or the sibling `go-*` campaign's PR #325 merges first; both were cut from `main` before either merged — GitHub issue #326, sub-issues #327-#337, PRs #330-#342) — sibling to the just-completed `go-*` domain rebuild, run as the second half of the same recursive, autonomous batch request. Implementation sub-issues grouped by related skill cluster (foundations, component/testing, data/viz, cross-cutting — 4 groups instead of 13 independent campaigns), matching the Go campaign's scale adaptation. **Two new React books researched in parallel** with the Go campaign's four, landing in a new `research/frontend-engineering/` cluster (the only prior React-adjacent coverage, `research/micro-frontends/`, is scoped to Module Federation composition mechanics, not general React idiom): *Learning React* (Banks & Porcello) and *React Design Patterns and Best Practices* (Roldán). Unlike the Go campaign's confirm-coverage sub-issue (which found two previously-catalogued findings had silently never been applied), this campaign's equivalent sub-issue found no such backlog — both new books' findings checked out cleanly against current reality on independent re-verification, a genuinely different and honestly-reported outcome rather than a copy-pasted process producing an artificially similar result. **Most consequential fix**: `react-graph-visualization` had two real bugs, both independently re-verified by direct file read before being treated as real (not trusted from the research agent's report alone) — a graphology `Graph` instance created with `useMemo(() => new Graph(), [])` but imperatively mutated on every `expand()` call (`useMemo` is a discardable React performance hint, not a lifetime guarantee — fixed to a lazy-init `useRef`), and a Sigma-instance cleanup effect referencing a `worker` variable declared in an entirely separate, disconnected code snippet — not in scope of that effect and not in its dependency array, so the two published snippets never actually composed into a correct implementation. Consolidated into one coherent worked `EstateGraph` component showing the graph ref, worker ref, and Sigma renderer all created and torn down together in a single mount effect, replacing three previously-scattered fragments. Other real fixes: `eslint-plugin-react-hooks` added to `react-project-structure`'s lint configuration (`rules-of-hooks` at `error`, `exhaustive-deps` at `warn` with an explicit rationale for the severity split) plus a matching `agents/frontend-engineer.md` directive and checklist line — mechanically enforcing the Rules of Hooks that were previously stated correctly in prose but never linted for; `react-component-design`'s render-props section rewritten from a single uncoded bullet into a worked `VirtualizedList` example correctly framing custom hooks as the more idiomatic default for most reuse problems while preserving render props for its genuine use case (caller-controlled rendering). One of four groups (cross-cutting: `react-accessibility`, `react-observability`, `react-performance-optimization`, `react-dockerfile`) closed with **zero file changes** — every research hit for that group was a positive confirmation, not a gap — reported honestly rather than manufacturing content, matching the Go campaign's tooling-group precedent. 6 new contract tests added, each live-verified and grounded in a fact confirmed absent from its target `SKILL.md` via grep before finalizing. 2 new glossary terms (Compound Component, Rules of Hooks). A real mid-campaign recovery is worth noting: `plan_exit.py` partially failed while creating the discovery sub-issues (one issue's GitHub-side creation succeeded but its local-state tracking was never saved before the process errored on the next item) — diagnosed by checking GitHub issue numbers directly against local state, fixed by manually reconstructing the missing state entry to match GitHub's ground truth, then safely re-running `plan_exit.py`, which correctly skipped the already-created issue and created the remaining two fresh, with no duplicates. See decision D037. Per Shafi's default, the final integration-to-main PR is left open for his own review, not auto-merged.
- 2026-07-26 — `skills/react-project-structure/SKILL.md` deep-rebuilt (v2.0.0 → v3.0.0 MAJOR; issue #394, PR #408) per Shafi's corrective feedback after the initial Chunk 44 campaign. First progressive-disclosure split: new `references/` files covering module-federation-vite-config, eslint-and-lint-staged-standard, typescript-path-alias-standard, and testing-infrastructure. `SKILL.md` body trimmed to decision-shaping guidance only; exhaustive tool config and worked examples moved to references. Contract test added, live-verified.
- 2026-07-26 — `skills/react-component-design/SKILL.md` deep-rebuilt (v2.0.0 → v3.0.0 MAJOR; issue #395, PR #409) per Shafi's corrective feedback. First progressive-disclosure split: new `references/worked-examples.md` (compound-component Tabs, render-props VirtualizedList, custom-hook reuse examples) and `references/composition-patterns.md`. The render-props section — previously a single uncoded bullet — rewritten into a worked example correctly framing custom hooks as the more idiomatic default while preserving render props for its genuine use case (caller-controlled rendering). Contract test added, live-verified.
- 2026-07-26 — `skills/react-state-management/SKILL.md` deep-rebuilt (v2.0.0 → v3.0.0 MAJOR; issue #396, PR #410) per Shafi's corrective feedback. First progressive-disclosure split: new `references/cross-fragment-state-standard.md` (the shell-context-and-custom-events boundary rule, disambiguating what can and cannot cross a Module Federation boundary) and `references/tanstack-query-patterns.md`. Contract test added, live-verified.
- 2026-07-26 — `skills/react-api-client/SKILL.md` deep-rebuilt (v1.1.0 → v2.0.0 MAJOR; issue #397, PR #411) per Shafi's corrective feedback. First progressive-disclosure split: new `references/typed-client-generation.md` (openapi-typescript + openapi-fetch codegen, justified against orval/openapi-generator), `references/error-mapping-standard.md` (full AppError discriminated union, ErrorResponse envelope verified against go-chi-handler's actual current content), `references/auth-token-standard.md` (single-flight refresh-on-401 preventing N concurrent 401s from racing token rotation), `references/cancellation-standard.md` (AbortController cross-referencing react-state-management's TanStack Query cache). Flagged a pre-existing inconsistency between go-chi-handler's `fields`/`traceId` envelope and api-contract-design's OpenAPI schema — noted honestly, not silently resolved. Contract test added, live-verified.
- 2026-07-26 — `skills/react-routing/SKILL.md` deep-rebuilt (v2.0.0 → v3.0.0 MAJOR; issue #398, PR #412) per Shafi's corrective feedback. `references/cross-remote-composition.md` deepened (132 → 170 lines): full route-tree ownership standard (shell-vs-remote table and litmus test), host/protected-route/remote route-tree code moved out of SKILL.md. `references/url-state-and-params.md` deepened (65 → 129 lines): three-way route-param/search-param/component-state decision test, deep-linking correctness as a four-part pass/fail check (direct entry, refresh, share, back/forward). New `references/remote-load-failure-handling.md` (146 lines): full retry-with-backoff standard distinguishing transient causes (worth retrying) from deterministic ones (a strictVersion conflict, not worth retrying), a shell-owned RemoteLoadError fallback distinct from React's own error-boundary fallback. Contract test added, live-verified.
- 2026-07-26 — `skills/react-graph-visualization/SKILL.md` deep-rebuilt (v1.2.0 → v2.0.0 MAJOR; issue #399, PR #413) per Shafi's corrective feedback. The two previously-verified real bugs from Chunk 44 — a `useMemo`-vs-`useRef` lifetime bug on the mutated graphology instance and an out-of-scope worker variable in a cleanup effect — consolidated into a single coherent `EstateGraph` worked example in new `references/estate-graph-worked-example.md` (129 lines). New `references/rendering-performance-and-lod.md` (111 lines): WebGL-vs-SVG render-budget thresholds and level-of-detail/culling standard. New `references/worker-lifecycle-and-interaction-state.md` (113 lines): six-point worker-lifecycle checklist, interaction-state ownership standard (camera/hover in Sigma's own state, selection/filters promoted to URL). Contract test added, live-verified.
- 2026-07-26 — `skills/react-dashboard-components/SKILL.md` deep-rebuilt (v1.1.0 → v2.0.0 MAJOR; issue #400, PR #414) per Shafi's corrective feedback. New `references/data-density-and-virtualization-standard.md` (81 lines): concrete 200-row virtualization threshold, cross-references react-component-design's actual VirtualizedList render-props example. New `references/chart-accessibility-standard.md` (103 lines): Recharts-vs-visx selection criteria, the table-alternative requirement deepened into an exact standard — every chart, no exceptions, real `<table>` with scope headers. New `references/widget-state-standard.md` (112 lines): discriminated-union status model, sized-skeleton loading, empty-vs-error distinction. New `references/realtime-update-standard.md` (114 lines, entirely new content): keyed-patch diffing discipline for live widgets, throttling, connection-health signal. Contract test added, live-verified.
- 2026-07-26 — `skills/react-accessibility/SKILL.md` deep-rebuilt (v1.1.0 → v2.0.0 MAJOR; issue #401, PR #415) per Shafi's corrective feedback. New `references/keyboard-and-focus-management.md` (65 lines): pre-existing modal focus-trap effect preserved and deepened with the exact focus-return-to-trigger-never-body rule, unmounted-trigger fallback guard, and `tabIndex={-1}` mechanics. New `references/aria-and-semantic-html-standard.md` (87 lines): the precise First Rule of ARIA statement and native-vs-ARIA-gap decision table. New `references/contrast-and-motion-standard.md` (121 lines): exact WCAG 2.1 AA contrast ratios (4.5:1/3:1/3:1), `prefers-reduced-motion` handling honestly flagged as adopted practice beyond strict AA rather than fabricated as an AA requirement. Quality Criteria rebuilt as a WCAG-success-criterion-by-criterion table (10 criteria) each with a mechanical pass/fail check. 3 new canonical glossary terms (ARIA, Core Web Vitals, WCAG). Contract test added, live-verified.
- 2026-07-26 — `skills/react-dockerfile/SKILL.md` deep-rebuilt (v1.1.0 → v2.0.0 MAJOR; issue #402, PR #416) per Shafi's corrective feedback. New `references/multi-stage-build-standard.md` (152 lines): full builder + nginx-unprivileged Dockerfile, `.dockerignore`, nginx caching/compression/SPA-fallback config, plus a new CORS-for-remoteEntry.js standard — a genuine Module-Federation-specific serving gap the original skill never covered. New `references/runtime-config-injection.md` (121 lines): the Vite build-time-constant-folding problem stated precisely and tied to the build-once-promote-the-same-artifact CI/CD principle. New `references/image-size-and-security-standard.md` (130 lines): node_modules-vs-dist size reasoning, side-by-side security table matched against go-dockerfile's actual content (distroless/nonroot/uid 65532/Cosign/Trivy), adapted to nginx-unprivileged/uid 101. Contract test added, live-verified.
- 2026-07-26 — `skills/react-component-testing/SKILL.md` deep-rebuilt (v1.1.0 → v2.0.0 MAJOR; issue #403, PR #417) per Shafi's corrective feedback. New `references/query-priority-and-interaction-standard.md` (74 lines): full query-priority rationale per rung, worked `userEvent`-vs-`fireEvent` comparison showing the real keydown/keypress/input/keyup event-sequence difference `fireEvent`'s synthetic single-event dispatch misses. New `references/mocking-and-snapshot-standard.md` (83 lines): MSW mock-boundary standard explaining why interception happens at the network layer, snapshot-testing appropriate-vs-anti-pattern criteria (new content, previously absent). Cross-references verified against actual current content on this branch, not assumed future state. Contract test added, live-verified.
- 2026-07-27 — `skills/react-e2e-testing/SKILL.md` deep-rebuilt (v1.1.0 → v2.0.0 MAJOR; issue #404, PR #418) per Shafi's corrective feedback. New `references/cross-remote-journey-standard.md` (166 lines): full Playwright worked journey proving the shell + remotes composition itself — classify a data asset in one fragment, observe the visible effect in a different compliance-dashboard fragment — plus network-strategy/auth-setup/in-flow-a11y code. New `references/flakiness-and-visual-regression-standard.md` (119 lines): condition-based waits/`expect.poll` standard, quarantine policy, a reasoned in/out-of-scope call on visual regression using Playwright's built-in `toHaveScreenshot` (no new paid tool). Corrected a wrong assumption in the task brief itself: the brief assumed go-e2e-test's environment was a kind cluster via Helm, but that skill's actual current content says Testcontainers/docker-compose — grounded on the real text, not the brief's guess. Contract test added, live-verified.
- 2026-07-27 — `skills/react-performance-optimization/SKILL.md` deep-rebuilt (v1.1.0 → v2.0.0 MAJOR; issue #405, PR #419) per Shafi's corrective feedback. First progressive-disclosure split and the tightest body of the individual-rebuild campaign (119-line SKILL.md body). New `references/profiler-workflow-and-memoization-standard.md` (92 lines): numbered Profiler procedure (record, find longest commit bar, read why-did-this-render reasons, only then decide, re-measure), full worked broken-then-fixed `React.memo` shallow-comparison example — `DataAssetRow` defeated by inline style/arrow-function literals, fixed by hoisting/`useCallback`. New `references/memory-leak-and-concurrent-scheduling.md` (55 lines): memory-leak and `useDeferredValue`/`useTransition` worked examples moved out of SKILL.md body. Code-splitting and virtualization sections replaced with accurate cross-references to react-routing's Standard 2 and react-dashboard-components's 200-row virtualization threshold. Contract test added, live-verified.
- 2026-07-30 — `skills/react-observability/SKILL.md` deep-rebuilt (v2.0.0 → v3.0.0 MAJOR; issue #406, PR #420) per Shafi's corrective feedback. SKILL.md rebuilt to five standards (238-line body) from four: new Standard 0 (initialization order — Sentry before OTel before `React.render` before web-vitals, so no error or span is lost at startup) and Standard 5 (session correlation — `session.id` anchoring spans and Sentry events, never a metric label). Extended `references/error-boundary-and-reporting-standard.md`: `initErrorReporting()` env gate, `session.id` in `reportClientError`, §4 sourcemap upload via `@sentry/vite-plugin` (`deleteAfterUpload: true` — sourcemaps must not be served from the CDN), §5 network-error filtering in `beforeSend` (`TypeError`/fetch, `AbortError`, browser extensions). Extended `references/tracing-and-web-vitals-standard.md`: full `initTracing()` with resource attributes using semantic convention constants, §4 `TraceIdRatioBasedSampler` (why ratio-based preserves complete traces — all spans in a trace share the same sampling decision), §5 per-remote scoped tracers via `trace.getTracer()` — not `provider.register()` which overwrites the global and severs in-flight spans — with Module Federation `shared:` singleton config, §6 explicit histogram bucket boundaries tuned to Good/Needs-improvement/Poor thresholds, §7 `MeterProvider` with OTLP/HTTP, §8 shutdown (`pagehide` handler) and HMR guard. New `references/telemetry-initialization-and-testing.md` (334 lines): pinned package manifest with OTel family minor-version alignment warning, full `main.tsx` init chain with failure-mode table, `session.ts` with `clearSessionId()` on logout, four testing patterns (`InMemorySpanExporter` with `SimpleSpanProcessor`, `scrubEvent` null-returns, web-vitals mock, env-gate). Contract test updated to probe the per-remote `provider.register()` overwrite rule.
- 2026-07-30 — `skills/skill-authoring-standards/SKILL.md` rebuilt (v1.0.0 → v1.1.0 MINOR; Chunk 46, GitHub issue #422, PR #423) — the first use of `skill-authoring-standards` to rebuild itself. New `scripts/validate-skill-structure.sh` (governance script, 15/15 synthetic tests in `tests/scripts/validate-skill-structure.test.sh`): checks 7 required frontmatter fields in canonical order, body line count ≤200, and bidirectional `references/` pointer integrity (body mentions file ↔ file exists on disk). New `references/vocabulary-and-collision-detection.md`: formally defines the six named terms used throughout the skill body (Trigger Surface, Resident Content, Skill Collision, Progressive Disclosure, Declared Composability, Self-Contained Reference) plus Periodic Collision Detection guidance with a known-adjacent skill pairs table (5 pairs). New `assets/skill-template.md`: minimal copyable SKILL.md starter with all 7 required frontmatter fields in canonical order, the four-section body structure, and placeholder commentary for drafting the description as a trigger surface. SKILL.md body: added `related: [glossary-management, methodology-review]` frontmatter field; added vocabulary pointer in Purpose section; added starter-template and structural-validation pointers in Applying section; fixed two false-positive `references/<file>.md` patterns in prose examples that fooled the validator's grep. Contract test updated from a body-answerable question (200-line threshold) to a question answerable only from the references file (Skill Collision symptom: "answers questions it does not own"), proving the progressive-disclosure split is followed.
- 2026-07-25 — All 24 `go-*` `backend-engineering` skills rebuilt under `skill-authoring-standards` as one grouped, autonomous campaign (Chunk 47, GitHub issue #298, sub-issues #299-#316, PRs #304-#324) — requested directly by Shafi as a recursive batch refactor across every `go-*`/`react-*` skill, run fully autonomously per his explicit instruction. Scaled the established single-skill two-wave process to domain scale: implementation sub-issues grouped by related skill cluster (core service, domain/data, event-driven, cross-cutting, tooling, testing part 1, testing part 2 — 7 groups) rather than one campaign per skill, which would have produced roughly 170 GitHub sub-issues/PRs for this domain alone. **Four new Go books researched via parallel background agents** (a deliberate parallelization of independent research, distinct from Wave 1's usual sequential-foreground convention within a single skill): *The Go Programming Language* (Donovan & Kernighan), *100 Go Mistakes and How to Avoid Them* (Harsanyi), *Concurrency in Go* (Cox-Buday), *Let's Go / Let's Go Further* (Edwards) — the roster previously had only one survey-level Go book. A confirm-and-consolidate discovery sub-issue found that two previously-catalogued findings (a CORS middleware gap, incomplete `http.Server` timeout configuration) had never actually been applied despite being researched in an earlier pure-research chunk, plus two more already-researched-but-unapplied findings (Clean Architecture's Dependency Rule/Humble Object naming, Khorikov's four-pillars/quadrant testing framework) via a fresh grep sweep. **Most consequential fix**: `go-error-handling` gained the nil-concrete-value-in-a-non-nil-`error`-interface footgun — one of Go's most-cited mistakes, previously entirely undocumented — with its wrong/right worked example independently compiled and run against a live Go 1.23.4 toolchain (installed mid-session) to verify the bug reproduces exactly as described, not just reasoned about. Other real, verified fixes: `go-middleware` gained CORS middleware (positioned before `Authenticate`) and `Connection: close` on panic recovery; `go-service-skeleton` completed its `http.Server` timeout fields; `go-event-publisher`'s `drainOnce` was fixed to preallocate slices against a known bound (a genuine self-inconsistency against this repo's own `go-performance-optimization` rule); `go-concurrency-patterns` gained named deadlock/livelock/starvation vocabulary with the critical caveat that neither Go's race detector nor its deadlock detector catches a partial deadlock or livelock; `go-chaos-test` gained a matching experiment row, the only place in the toolchain that would actually catch one; `go-domain-model`, `go-repository-pattern`, and `go-service-layer` gained Humble Object and Confinement naming for already-correct-but-unnamed patterns. 11 of 24 skills received their first `references/` split. 13 new contract tests added, each live-verified and grounded in a fact confirmed absent from its target `SKILL.md` via grep before finalizing (two needed a fix for markdown-formatting/case-sensitivity substring mismatches, not skill defects). Two groups (tooling; most of testing part 2) had no direct research finding and were handled honestly — light shape/consistency work only, explicitly reported as calibrated-thin rather than padded to match the other five groups' depth. See decision D039. *(Renumbered from Chunk 44 to 47 at merge time — both the go-* and react-* grouped campaigns were cut from main before either PR merged; the react-* PR merged first.)*
- 2026-07-26 — **Correction**: all 24 `go-*` `backend-engineering` skills rebuilt again, individually this time, on the same `issue-298-go-skills` integration branch (Chunk 48, GitHub issue #298, sub-issues #344-#368, PRs #369-#393) — Shafi reviewed Chunk 47's grouped-cluster pass and rejected it explicitly: `SKILL.md` files ran past the `skill-authoring-standards` ~150-line progressive-disclosure target, Output Format sections were bare file/folder listings instead of real per-artifact engineering standards, and Quality Criteria sections were small tables instead of genuinely comprehensive, mechanically-checkable standards. He instructed one dedicated sub-issue and branch per skill going forward, with GitHub-issue/PR count explicitly not a cost he is optimizing for. Every skill got a real `references/` progressive-disclosure split (11 already had one from Chunk 47; all 24 now have multi-file reference trees) covering, wherever relevant to that skill's artifact type: error handling and error-message standards, concurrency, unit/integration/contract-test standards, and memory/execution-time optimization. Chunk 47's real technical fixes (the nil-interface footgun, `drainOnce`'s preallocation fix, the CORS/timeout completions, the deadlock/livelock vocabulary) were preserved and deepened, not discarded. **Cross-skill consistency verification caught real bugs**: `go-middleware`'s rebuild found and fixed a bug the grouped pass had introduced — `RequestID` minted its own private context key incompatible with the key `go-chi-handler`'s `writeError` actually reads via chi's own `middleware.GetReqID`, which would have left `traceId` silently empty on every error response; `go-integration-test`'s rebuild caught a quadrant-numbering mismatch in its own sub-issue brief against `go-unit-test`'s actual published table before writing a citation that would otherwise have been silently wrong; `go-load-test`'s rebuild replaced self-invented SLO numbers with the real ones from `slo-definition` rather than inventing a second, competing set. 9 new contract tests added for skills that had none before; one pre-existing test (`go-event-consumer`) had its premise rewritten after a legitimate design correction (the liveness heartbeat's single-ticker design replaced with Cox-Buday's actual two-signal Heartbeat Pattern, which the single-ticker version didn't really implement) and rerun live. 5 new canonical glossary terms (Fitness Function, Expand/Contract Pattern, Blast Radius, Steady-State Hypothesis, Provider State), each verified to recur across multiple rebuilt skills before being added. 10 total new canonical glossary terms for the go-* campaign (5 from Chunk 47: Table-Driven Test, Humble Object, Dependency Rule, Confinement, Heartbeat Pattern; 5 from Chunk 48). See decision D040. *(Renumbered from Chunk 45 to 48 at merge time, same go-*/react-* branch-sequencing reason as Chunk 47.)*
- Content improvement campaign across all skills, agents, and repo metadata: unified component frontmatter schemas, restored always-on rule wiring in the five newest agents, fixed dead references, terminology drift, stale metadata, and code currency issues. Delivered as five batch PRs (PR #29–#33).
- CLAUDE.md, README.md, `.claude-plugin/plugin.json`, `settings.json` corrected from an untested conceptual spec (fictional lifecycle events, invalid manifest fields, "Tools" as a component type) to real, verified Claude Code mechanics (Chunk 19).
- `agents/<role>/AGENT.md` restructured to flat `agents/<role>.md` — the nested form is not discovered by Claude Code's real agent-discovery mechanism (confirmed empirically; folded into Chunk 19).
- All 136 skills restructured from `skills/<domain>/<name>/SKILL.md` (domain-grouped, confirmed not discovered by Claude Code) to flat `skills/<name>/SKILL.md` (verified discoverable) — the same class of bug as the agents fix above, closing `sdlc-context.json`'s open question Q001. Domain grouping survives only as a logical tag in `sdlc-context.json`'s `skill_domains`, not as a filesystem path. Zero name collisions across the 15 former domains, so no skill was renamed. ~17 literal cross-references to the 4 governance skills (`glossary-management`, `methodology-review`, `artifact-manifest`, `sdlc-config-management`) updated to their new flat paths; `naming-convention-enforcer.sh`'s path regex updated to match.

## [0.1.0] — 2026-06-24 → 2026-06-26

Foundational build, delivered as one chunk per PR (Chunks 1–14 of the build checklist).

### Added
- 2026-06-24 — Skeleton folder structure: all plugin directories established (Chunk 1, PR #1)
- 2026-06-24 — `sdlc-context.json` factory memory and session state (Chunk 2, PR #1)
- 2026-06-24 — Plugin manifest, `CLAUDE.md` always-on standards, `settings.json` (Chunk 3, PR #1)
- 2026-06-24 — Canonical Ubiquitous Language glossary (~120 terms) and methodology review standards: `governance/` skills `glossary-management`, `methodology-review` (Chunk 4)
- 2026-06-24 — `strategy/` skills (8) and `product-strategist` agent (Chunk 5, PR #2)
- 2026-06-24 — `discovery/` skills (11) and `requirements-analyst` agent (Chunk 6, PR #20)
- 2026-06-25 — `domain-modeling/` skills (9) and `domain-modeler` agent (Chunk 7, PR #21)
- 2026-06-25 — `architecture/` skills (9) and `enterprise-architect` agent (Chunk 8, PR #22)
- 2026-06-25 — `security/` skills (9), `security-architect` and `security-engineer` agents (Chunk 9, PR #23)
- 2026-06-25 — `ux/` skills (4) and `ux-architect` agent (Chunk 10, PR #24)
- 2026-06-25 — `data-architecture/` skills (7) and `data-architect` agent (Chunk 11, PR #25)
- 2026-06-25 — `backend-engineering/` skills (16), `observability/` instrumentation skills (4), and `backend-engineer` agent (Chunk 12, PR #26)
- 2026-06-26 — `frontend-engineering/` skills (14) and `frontend-engineer` agent (Chunk 13, PR #27)
- 2026-06-26 — `test-engineering/` skills (12) and `test-strategist` agent (Chunk 14, PR #28)
- 2026-06-29 — MIT LICENSE file
