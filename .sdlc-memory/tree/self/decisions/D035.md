# D035: Rebuild bdd-feature-file (Chunk 42, issue #266) as a modest content addition -- one new references/ file, one scaffold/validate script pair, one asset template -- grounded in new research/testing/specification-by-example-adzic.md (Gojko Adzic) plus consolidation of four already-existing tangential research files. Added the Three Amigos naming (Crispin & Gregory's finding, sharpened by Adzic's checkable Specification Workshop definition-of-ready), the missing half of Living Documentation's actual definition (Adzic's most consequential finding for this skill), the Key Examples minimality principle, and one Anti-Patterns row for untended specification growth. Added 5 new canonical glossary terms (Feature File, Given/When/Then, Living Documentation, Scenario, Specification Workshop) closing a completeness gap: three of the five were already named in CLAUDE.md's own canonical Testing ubiquitous-language table but had no standalone glossary headword.

**Date:** 2026-07-24

**Rationale:** Wave 1 discovery's #268 sub-issue confirmed the skill already fully implements Ken Pugh's ATDD declarative-vs-imperative point (a provenance note, not new content) and that Cohn/Lee/Jorgensen's research has no direct finding for this skill. The #267 sub-issue's new Adzic research found the skill's existing Living Documentation section was a faithful but partial implementation of Adzic's own, more precise definition -- the section already captured 'current because tested in CI' but never named the second half (mechanically deriving a browsable documentation artifact from the executed suite), which is the actual payoff Adzic's practice is named for. This is the second skill-refactor effort (after artifact-manifest, D034) where Wave 1's two discovery sub-issues split cleanly into 'confirm existing coverage' and 'new book research' running in the same wave, rather than one sub-issue serving both jobs -- appropriate here because a genuine, well-justified new-book target existed (an already-used-but-uncredited glossary term) alongside real existing tangential coverage worth consolidating first.

**Alternatives rejected:**
- Treat the Three Amigos naming gap alone as sufficient grounds for the rebuild, skipping new Adzic research -- rejected: 'Specification by Example' was already a load-bearing, cited glossary term with no source book ever researched, and BDD is a non-negotiable CLAUDE.md methodology with (until this chunk) zero primary-source grounding for the skill that implements it -- the same category of gap api-contract-design's rebuild (D032) closed for its own domain
- Fold the Key Examples minimality principle into the existing Golden Triangle section as a rule change rather than a complementary addition -- rejected: the two operate on different axes (breadth-of-angles vs economy-of-examples-per-angle) per the research file's own explicit finding; conflating them would misrepresent Adzic's actual, narrower claim
- Skip the scaffold/validate script pair since the content findings were modest -- rejected: unlike artifact-manifest (where an existing JSON Schema and test already covered validation), bdd-feature-file's .feature output had no existing mechanical check anywhere, and the Golden-Triangle-tag/imperative-leakage/multi-When checks are all genuinely new, checkable ground worth automating
