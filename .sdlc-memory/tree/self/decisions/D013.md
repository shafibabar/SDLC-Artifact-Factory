# D013: Agents restructured from agents/<role>/AGENT.md (nested) to agents/<role>.md (flat) — folded into Chunk 19's PR. Confirmed empirically: a nested AGENT.md is not discovered by Claude Code's real agent-discovery mechanism; a flat <name>.md is.

**Date:** 2026-07-20

**Rationale:** Discovered while live-testing Chunk 19's commands via `claude plugin details` — Agents (0) despite 13 real agent files existing. Isolated the root cause with a controlled test copy before changing anything. Mechanical fix (file relocation only, no content change), low risk, same root-cause class as the commands/hooks fix already underway.

**Alternatives rejected:**
- Defer to a separate PR — rejected only for this specific fix since it was small, mechanical, and directly related; the same testing methodology surfaced a second, much larger issue (see open_questions) which WAS deferred
