# D037: Rebuild all 13 react-* frontend-engineering skills (Chunk 44, issue #326 -- provisional numbering, see that chunk's note_on_numbering) as a grouped campaign sibling to the go-* rebuild (D037's chunk number collides with the Go campaign's own D037 for the same reason -- both branches were cut from main before either PR merged; resolve at merge time per the established Chunk 39/40 precedent). Two new React books researched in parallel with the Go campaign's four (Learning React -- Banks & Porcello; React Design Patterns and Best Practices -- Roldan), landing in a new research/frontend-engineering/ cluster. Fixed two real, independently-verified bugs in react-graph-visualization (a useMemo-vs-useRef lifetime bug on a mutated graphology instance; a cleanup effect referencing an out-of-scope worker variable from a disconnected snippet), added eslint-plugin-react-hooks mechanical enforcement to react-project-structure and agents/frontend-engineer.md, and rewrote react-component-design's render-props section to correctly frame custom hooks as the more idiomatic default while preserving render props for its genuine use case (caller-controlled rendering). One of four groups (cross-cutting) closed with zero file changes -- all research hits were positive confirmations, not gaps.

**Date:** 2026-07-25

**Rationale:** Same grouped-campaign rationale as the sibling Go decision (D037's Go entry): batching 13 skills into 4 cluster sub-issues instead of 13 independent campaigns kept the GitHub footprint tractable while preserving full discovery-before-implementation rigor. The confirm-coverage sub-issue's finding (#329) is itself notable: unlike the Go campaign, which found a real backlog of previously-researched-but-unapplied content, this campaign found the React domain's existing skills already largely reflect both new books' guidance correctly on first read -- a genuinely different, calibrated-honest outcome from the sibling campaign, not a copy-pasted process producing an artificially similar-shaped result. The react-graph-visualization bug fix is the campaign's highest-value finding specifically because it was independently re-verified by direct file read (not trusted from the research agent's self-report alone) before being treated as real -- consistent with this whole two-wave process's standing 'verify, don't just trust the agent' discipline.

**Alternatives rejected:**
- Treat the cross-cutting group's lack of findings as a signal to skip that sub-issue entirely rather than running it and reporting zero changes -- rejected: running the sub-issue and documenting a genuine 'nothing applies here' finding is itself valuable, auditable evidence that the group was actually checked, not silently skipped; matches the Go campaign's own tooling-group precedent (#313) for the identical situation
- Fix react-graph-visualization's two bugs as two separate, minimal patches to the existing three scattered snippets rather than consolidating into one worked example -- rejected: the root cause of the second bug (worker out of scope) was structural, not a typo -- three snippets describing one interdependent lifecycle will keep drifting apart under future edits unless consolidated into a single source of truth for that lifecycle, which is what was done
- Wait for the Go campaign's PR to merge to main before starting the React campaign, avoiding the chunk-number collision entirely -- rejected: PR #325 was deliberately left open for Shafi's own review per this repo's standing default, with no defined timeline for when that review happens; blocking the React campaign on an indefinite wait would contradict the explicit instruction to proceed autonomously through both domains now, and the chunk-collision-at-merge-time pattern is already an established, low-cost resolution path in this repo's history
