# D034: Rebuild artifact-manifest (Chunk 41, issue #251) as an evidence-based restructuring of its own existing content -- thin SKILL.md + references/artifact-type-catalog.md + references/manifest-worked-example.md + assets/manifest-instance-template.json -- rather than a content addition grounded in new book research, since Wave 1 discovery (sub-issue #252) confirmed no book in the 50-book research/ corpus is squarely about software/documentation artifact registries or traceability matrices. Declined a skill-owned scaffold/validate script for this skill: schemas/sdlc-manifest.schema.json and tests/schemas/sdlc-manifest.test.py already formalize and test the per-product instance shape, so a script would duplicate existing coverage rather than fill a gap.

**Date:** 2026-07-24

**Rationale:** Every prior skill-refactor effort's Wave 1 discovery either found a genuinely new book to research or confirmed an existing book's coverage already applied to the target skill. This is the first case where discovery confirmed neither -- the skill's actual subject matter (a plugin-internal artifact registry) sits outside every researched cluster's scope, including the data-governance-and-mdm cluster's closest tangential content (DAMA-DMBOK Ch. 12 Metadata Management, Loshin's MDM ID/lifecycle concepts), which are both scoped to governing data assets, not SDLC/documentation artifacts. Proceeding without forcing a false-positive research grounding, and instead restructuring the skill's own already-sound content into the shape sibling governance skills (ddd-agent-handoff, glossary-management, methodology-review) already settled on, keeps the skill-authoring-standards rebuild honest about what discovery actually found, per the standing 'calibrated honesty' norm (see the research-books-inventory memory's note on the 16-book batch's low-candidate-count books).

**Alternatives rejected:**
- Force-fit DAMA-DMBOK's Metadata Management chapter as grounding content for artifact-manifest, since it is the closest thematic match in the corpus -- rejected: that chapter's own Applies To table already scopes its findings to data-asset-governance skills (data-classification, canonical-data-model, etc.), not to this skill, and citing it here would misattribute content the skill's rebuild did not actually derive from it
- Skip the rebuild entirely since no new research grounds it -- rejected: the skill was still a flat 195-line file past the point where sibling governance skills already adopted progressive disclosure, and the skill-authoring-standards shape rubric applies independent of whether a specific rebuild happens to be research-driven
- Add a skill-owned scaffold/validate script pair to match every prior rebuilt skill's pattern -- rejected as scope creep: this skill's output is validated by an existing JSON Schema (schemas/sdlc-manifest.schema.json) and existing test (tests/schemas/sdlc-manifest.test.py) already, so a redundant script would duplicate coverage rather than close a real gap
