# D022: Standardize on Miro for visual/board-style artifacts across skills and agents, but as structured board specs only for now (a new miro-board-notation skill defining Miro's real element vocabulary — sticky note, shape, frame, connector, text — and a two-table spec format), not a live API/MCP integration. The same MVP-dependency-pull pattern from D021 applied again: miro-board-notation was pulled into the subdomain-distillation refactor plan as its own sub-issue (#69) because 3 dependent files (a Wardley Map, a Domain Vision Board, and the classification canvas) needed it to exist.

**Date:** 2026-07-22

**Rationale:** Checked via claude mcp list before writing any Miro-related content: no Miro MCP/API connection exists in this environment, and this repo's mcp/ directory is an explicitly deferred placeholder per CLAUDE.md. Writing diagrams as Miro-vocabulary board specs now costs nothing extra (they're still buildable by hand or readable as structured tables) and means zero rework once a real Miro connection exists later — the alternative (plain prose diagrams now, retrofit to Miro's vocabulary later) would require rewriting every visual artifact this campaign produces in the meantime.

**Alternatives rejected:**
- Wire up a live Miro MCP/API connection now — rejected per Shafi's explicit choice; no account/token readiness signal was given, and premature integration risks building against the wrong API shape before more skills actually need to render boards
- Keep diagrams as plain prose/tables with no Miro-specific vocabulary — rejected: defeats the stated goal of standardizing on Miro, and would require retrofitting every visual artifact produced during this campaign later
