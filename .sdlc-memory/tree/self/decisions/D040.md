# D040: Corrected Chunk 47's grouped go-* rebuild (7 skill-cluster sub-issues) into 24 individual per-skill sub-issues (#344-#368), each with its own dedicated branch, executed sequentially against the same existing issue-298-go-skills integration branch and parent issue #298 -- not a new campaign. (Renumbered from D038 to D040 at merge time -- the go-* correction decision used D038 on the integration branch, but main's D038 is the react-* individual rebuild decision which merged first.)

**Date:** 2026-07-26

**Rationale:** Shafi reviewed Chunk 47's grouped-cluster pass and rejected it explicitly: SKILL.md files ran past the skill-authoring-standards ~150-line progressive-disclosure target, Output Format sections were bare file/folder listings instead of real per-artifact engineering standards, and Quality Criteria sections were small tables instead of genuinely comprehensive, mechanically-checkable standards. He stated directly that GitHub-issue/PR count is not a cost he is optimizing for and instructed one dedicated sub-issue and branch per skill regardless of count. Every skill was rebuilt again on top of (not discarding) Chunk 47's real technical fixes -- e.g. the nil-interface footgun, the drainOnce preallocation fix, the CORS/timeout completions were all preserved and deepened, not reverted.

**Alternatives rejected:**
- Leave Chunk 47's grouped depth as final and treat Shafi's feedback as guidance for future campaigns only -- rejected: he explicitly asked for this exact campaign's output to be corrected
- Start a fresh integration branch/parent issue for the correction -- rejected: issue #298 and issue-298-go-skills were still open and unmerged, so reusing them avoided a second parallel PR against the same 24 files
- Batch the 24 corrections into fewer sub-issues again for efficiency -- rejected outright per Shafi's explicit instruction; this was the exact grouping decision he was correcting.
- Discard Chunk 47's real technical fixes and rebuild from the pre-Chunk-47 baseline -- rejected: those fixes were independently verified correct and had no bearing on the depth complaint
