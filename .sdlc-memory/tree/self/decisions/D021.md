# D021: Established the skill-refactor campaign's per-skill procedure using ddd-agent-handoff as the first case: one parent GitHub issue per skill, dependency-ordered sub-issues (one per references/assets file plus a final rewrite-SKILL.md step and a housekeeping step), and a mandatory gap-check against real industry practice (here: Team Topologies' three interaction modes, and Eric Evans' Domain-Driven Design directly) before finalizing the design. When the gap-check surfaces a genuinely missing skill (subdomain-distillation, for Evans Part IV Distillation) rather than misplaced content, pull it into the same plan as its own MVP-scoped sub-issue if a dependent file needs to reference it, instead of leaving it as an unscheduled idea.

**Date:** 2026-07-22

**Rationale:** Shafi's stated goal for this campaign is skills that are 'result-oriented for a proper software organization,' not prose — a mechanical progressive-disclosure split alone doesn't guarantee that; checking against a named, credible industry standard (here, the source text DDD itself is named after) is what caught two real gaps (Core/Supporting/Generic Subdomain classification as the primary mode-selection input; the operational-pause vs. breakthrough-triggered-refactor distinction for resumed sessions) that a generic 'add more detail' pass would likely have missed. Pulling subdomain-distillation in as an MVP sub-issue (rather than deferring it) avoided handoff-protocol.md shipping with a pointer to a skill that didn't exist yet.

**Alternatives rejected:**
- Defer subdomain-distillation as a queued idea for its own future session — rejected once handoff-protocol.md's Mode Selection Criteria was designed to depend on it; a real dependency should not ship broken
- Skip the Evans gap-check and treat the Team-Topologies-anchored design as sufficient — rejected: Shafi explicitly asked to check against the source text before treating the plan as done, and it surfaced two real gaps, not just polish
