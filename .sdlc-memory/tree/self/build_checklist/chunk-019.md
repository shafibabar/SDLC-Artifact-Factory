# Chunk 19: All commands (phase drivers + cross-cutting) — real slash commands + meta-doc corrections

**Status:** complete

**Completed:** 

**Deliverables:**
- 15 real commands in commands/*.md, verified via claude --plugin-dir + live invocation
- CLAUDE.md/README.md/plugin.json/settings.json corrected to real Claude Code mechanics
- tools/ renamed to scripts/ throughout
- agents/<role>/AGENT.md restructured to flat agents/<role>.md (D013) — a second, larger instance of the same discovery issue (skills/) deferred, see open_questions Q001
