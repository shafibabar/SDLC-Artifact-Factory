# Chunk 47: Rebuild all 24 backend-engineering go-* skills under skill-authoring-standards, grouped campaign (issue #298) — renumbered from 44 at merge time

**Status:** complete

**Completed:** 2026-07-25

**Deliverables:**
- Requested directly by Shafi as an autonomous, recursive batch refactor of every go-* and react-* skill in the repo, explicitly authorizing operation without per-step confirmation. Scaled the established single-skill two-wave GitHub process (issue #251 onward) to a 24-skill domain by grouping related skills into one implementation sub-issue per cluster (7 groups) instead of one campaign per skill, which would have produced ~170 GitHub sub-issues/PRs for this domain alone -- same discovery-before-implementation discipline and per-skill rigor, different GitHub-issue granularity only.
- Wave 1 discovery (5 sub-issues, #299-#303): four new Go books researched via parallel background agents -- The Go Programming Language (Donovan & Kernighan), 100 Go Mistakes and How to Avoid Them (Harsanyi), Concurrency in Go (Cox-Buday), Let's Go / Let's Go Further (Edwards) -- the Go roster previously had only one survey-level book (designing-backend-systems-go-campbell.md). A confirm-and-consolidate sub-issue (#303) re-verified that skill re-derived research's two prior findings (CORS middleware, http.Server timeout completeness) were never actually applied despite being catalogued in an earlier pure-research chunk, and surfaced two more already-researched-but-unapplied findings (Clean Architecture's Dependency Rule/Humble Object naming, Khorikov's four-pillars/quadrant testing framework) via a fresh grep sweep -- a real process learning: prior book-batch research chunks were explicitly research-only with implementation deferred, and this pattern held across multiple still-open findings until this campaign closed them.
- Wave 2 (8 sub-issues, #309-#316) across 7 skill-cluster groups plus housekeeping. Most consequential fix: go-error-handling gained the nil-concrete-value-in-a-non-nil-error-interface footgun, one of Go's most-cited mistakes, previously entirely undocumented -- the worked wrong/right example was independently compiled and run against a real Go 1.23.4 toolchain (installed into scratchpad mid-session since none was preinstalled) to verify the bug reproduces exactly as described, not just reasoned about. Other real fixes: go-middleware gained CORS middleware (positioned before Authenticate) and Connection: close on panic recovery; go-service-skeleton completed its http.Server timeout fields; go-event-publisher's drainOnce was fixed to preallocate slices against a known bound, a genuine self-inconsistency against this repo's own go-performance-optimization rule; go-concurrency-patterns gained named deadlock/livelock/starvation vocabulary with the critical caveat that Go's own tooling (race detector, deadlock detector) catches neither a partial deadlock nor a livelock; go-chaos-test gained a matching experiment row, the only place in the toolchain that would actually catch one. 11 of 24 skills received their first references/ split; 13 new contract tests added, each verified live and grounded in a fact confirmed absent from the target SKILL.md via grep before finalizing. Two groups (tooling; testing part 2's non-chaos-test skills) had genuinely no direct research finding and were handled honestly -- shape/consistency work only, no manufactured content.
- 5 new canonical glossary terms (Table-Driven Test, Humble Object, Dependency Rule, Confinement, Heartbeat Pattern -- the last disambiguated explicitly from the existing Health Check entry, since both are liveness-adjacent but operate at different layers). This chunk's own housekeeping sub-issue (#316) closes the Go first-pass campaign; the correction campaign (Chunk 48) followed after Shafi's review rejection.
