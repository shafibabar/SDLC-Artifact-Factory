# Chunk 64: Python backend Tier 2 — pytest research + 8 python-* messaging & testing skills (epic #739, research-first, closed)

**Status:** complete

**Completed:** 2026-07-31

**Deliverables:**
- Second Python slice from the #695 spike: messaging + core testing. Owner backend-engineer; stack FastAPI + asyncpg + aiokafka.
- PHASE 1 — research (#745): research/testing/python-testing-with-pytest-okken.md (Brian Okken — pytest fixtures/scopes/conftest, parametrize, markers, pytest-asyncio, pytest-cov, testcontainers-python, fakes-over-mocks aligned with cosmic-python + Khorikov). Applies-To reviewed before Phase 2 — grounds the three testing skills and keeps the go-*/python-* testing symmetry.
- PHASE 2 — 8 new python-* skills (#740–#748, all v1.0.0, bodies ≤200, each mirrors its go-* sibling): python-event-consumer (aiokafka manual-offset + processed_events idempotency in-tx + Semaphore-I/O-vs-ProcessPool-CPU pool + DLQ), python-event-publisher (asyncio outbox poller, FOR UPDATE SKIP LOCKED, aiokafka tenant-keyed at-least-once), python-async-concurrency (asyncio.TaskGroup = errgroup analog, the GIL as a fundamental limit, ProcessPoolExecutor for true CPU parallelism with pickling cost — the widest go→python divergence), python-unit-test (pytest + pytest-asyncio + fakes-over-mocks), python-integration-test (testcontainers-python real Postgres/Redpanda, rollback-vs-fresh-tenant isolation), python-contract-test (schema-based consumer-driven, NO Pact broker — frugal, mirrors go-contract-test), python-performance-optimization (py-spy prod-sampling / scalene / tracemalloc, GIL-aware, measure-first, no sync.Pool), python-makefile (uv/ruff/mypy/pytest/build/migrate targets).
- 16 new references/ files, 8 contract tests. Each documents the honest Python-vs-Go divergences (GIL/ProcessPool, pytest fixtures vs Go table tests, py-spy prod sampling). All bodies came in ≤200 on the first pass (no python-dockerfile-style trim needed). Repo grew 169→177 skills and 169→177 contract tests (100% held). 19 python-* skills total (Tier 1 + Tier 2). All sub-issues #740–#748 closed, epic #739 closed. Merge-confirmation held; batch worktrees + branches pruned.
- REMAINING: Python Tier 3 (parity — python-grpc-handler, python-amqp-publisher/consumer, python-mongodb-repository, python-e2e-test, python-mutation-test, python-load-test, python-performance-test, python-chaos-test) is the last Python follow-on campaign, not yet started. With Tiers 1+2, a FastAPI service can be structured, implemented, persisted, messaged, and tested end-to-end; Tier 3 adds cross-protocol parity (gRPC/AMQP/Mongo) and the remaining test types. (Done in Chunk 65.)
