# Chunk 35: access-control-model rebuilt under skill-authoring-standards -- issue #165, sub-issues #166-#169

**Status:** complete

**Completed:** 

**Deliverables:**
- Checked skills/access-control-model/SKILL.md (v2.0.0, Chunk 30) against every research/ file mentioning access-control-model / access control before writing anything: Campbell (Designing Backend Systems with Go), Pragmatic Programmer, Secure by Design (existing primary source), Gale, DAMA-DMBOK. Consistent finding: no new content gap. DAMA-DMBOK's own file explicitly declines to propose a security skill -- 'Data Security is already owned thoroughly by the security cluster... adding a DAMA-flavored security skill would duplicate rather than fill a gap.' Pragmatic Programmer's Design by Contract note is conditional (only relevant if the term is ever added to the glossary), not a gap in this skill's own content. Campbell's file confirms this skill's ABAC model is deeper than a general Go backend text's JWT+RBAC baseline.
- skills/access-control-model/SKILL.md updated (v2.1.0, MINOR -- no content padding, only shape): tightened description naming the two new scripts, optional related: [skill-authoring-standards, subdomain-distillation, zero-trust-design] field, new Scripts section. Body is 238 lines (~218 excluding frontmatter), modestly over the 200-line split-threshold guidance -- left unsplit since the existing Attribute Categories/Permission Naming/Role Mapping tables are decision-shaping, not reference material a split would meaningfully lighten; skill-authoring-standards' own threshold is explicitly a prompt to apply the test, not a hard rule.
- New skills/access-control-model/assets/access-control-model-template.md (literal copy of the design-document format) and skills/access-control-model/scripts/scaffold-access-control-model.sh (generates a new design doc pre-filled with product metadata under artifacts/<product>/design/). Deliberately does NOT generate Go source files -- this skill's own Output Format is the markdown design document; the illustrative Go Domain Primitive code in references/domain-primitives-and-enforcement.md is teaching content for whichever agent later implements the design, not this skill's own artifact type.
- New skills/access-control-model/scripts/validate-access-control-model.sh (checks required frontmatter, and -- the more interesting check -- that the Domain Primitives/Policies/Per-Aggregate Trust-Boundary Decisions tables each have at least one real data row beyond their header, catching a design doc with the right section headings but empty tables). tests/scripts/scaffold-access-control-model.test.sh (6 assertions) and tests/scripts/validate-access-control-model.test.sh (5 assertions) added, synthetic scratch dir, no LLM call.
- tests/skills/access-control-model.contract.sh's question changed from the zero-value-trap detail (still correct, but unchanged since v2.0.0) to the v2.1.0-specific 'what generates a new design doc?' (expects scaffold-access-control-model.sh), live-verified via claude --plugin-dir before this sub-issue was marked complete.
- Executed via the issue-level integration-branch pattern (D025): issue #165, sub-issues #166-#169 branched from and merged into issue-165-refactor-access-control-model via exec_start.py --base/exec_complete.py's auto-merge-and-close, then that branch merged to main in one final reviewed PR. This completes the three-skill batch Shafi requested (acceptance-criteria #145, acceptance-sign-off #155, access-control-model #165), all following the adr-authoring pilot (#128) precedent.
