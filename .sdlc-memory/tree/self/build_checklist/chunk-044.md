# Chunk 44: Rebuild all 13 react-* frontend-engineering skills under skill-authoring-standards, grouped campaign (issue #326)

**Status:** complete

**Completed:** 2026-07-25

**Deliverables:**
- Sibling campaign to the go-* domain rebuild (issue #298), requested in the same autonomous, recursive batch instruction. Same 7-group-style scale adaptation, sized to 13 skills across 4 groups (foundations, component/testing, data/viz, cross-cutting) instead of 13 independent campaigns.
- Wave 1 discovery (3 sub-issues, #327-#329): two new React books researched via parallel background agents (spawned alongside the Go campaign's four, in the same efficiency batch) -- Learning React (Banks & Porcello) and React Design Patterns and Best Practices (Roldan) -- landing in a new research/frontend-engineering/ cluster, since the prior only React-adjacent coverage (research/micro-frontends/) is scoped to Module Federation composition mechanics, not general React idiom. Unlike the Go campaign's confirm-coverage sub-issue (#303), which found two previously-catalogued findings had silently never been applied, this campaign's confirm-coverage sub-issue (#329) found no such backlog -- both new books' findings checked out cleanly against current reality on first read.
- Wave 2 (5 sub-issues, #333-#337) across 4 skill-cluster groups plus housekeeping. Most consequential fix: react-graph-visualization had two real, independently-verified bugs -- a graphology Graph instance created with useMemo but imperatively mutated (useMemo is a discardable React performance hint, not a lifetime guarantee; fixed to a lazy-init useRef), and a Sigma-instance cleanup effect referencing a worker variable declared in a disconnected code block, not in scope and not in its dependency array (the two published snippets didn't compose) -- consolidated into one coherent worked example instead of three previously-scattered fragments. Other real fixes: eslint-plugin-react-hooks added to react-project-structure's lint config (rules-of-hooks at error, exhaustive-deps at warn with an explicit rationale) plus a matching agents/frontend-engineer.md directive and checklist line, mechanically enforcing the Rules of Hooks that were previously prose-only; react-component-design's render-props section rewritten from a single uncoded bullet into a worked example correctly distinguishing when render props (caller controls rendering) beat a custom hook (the more idiomatic default for most reuse problems). One group (cross-cutting: react-accessibility, react-observability, react-performance-optimization, react-dockerfile) had zero direct research findings -- all four hits were positive confirmations, not gaps -- and was closed with genuinely zero file changes, reported honestly rather than manufacturing content, matching the Go campaign's tooling-group precedent (#313).
- 2 new canonical glossary terms (Compound Component, Rules of Hooks). A real recovery mid-campaign: plan_exit.py partially failed creating the discovery sub-issues (created #327, silently failed on #328/#329 with no error surfaced beyond a truncated tool-output view) -- diagnosed by directly checking GitHub issue numbers against local state, found #327's issue+project-item both existed but were never recorded in local state's created-tracking dict (a crash between the GitHub mutation and the state.save call), fixed by manually reconstructing the missing state entry to match GitHub's ground truth before safely re-running plan_exit.py, which then correctly skipped the already-created #327 and created #328/#329 fresh -- no duplicate issues created.
