# Chunk 71: Architecture Review P4 — Skill De-duplication & Repointing COMPLETE, RESCOPED to duplication-detector precision (parent #1191, merged to main)

**Status:** complete

**Completed:** 

**Deliverables:**
- Fourth parent of the Architecture Review campaign. Integration branch arch-review/p4-duplication-precision -> main; 8 children (#1192-#1206), one issue + branch + PR each, per-child fresh-agent handoff.
- RESCOPE — the headline result. The charter estimated P4 at ~120-186 children of 'delete the restatement, repoint it to its canonical home'. Auditing the actual backlog BEFORE generating any issues showed it was ~99% FALSE POSITIVES. P4 was therefore rescoped to 8 children that fixed the DETECTOR rather than mutilating compliant skills. Not one line of compliant skill prose was deleted.
- WHY THE BACKLOG WAS FALSE: all 13 recurring-block clusters (62 skills) were the MANDATED artifact frontmatter (name/version/phase/owner/created) inside each skill's ## Output Format template — CLAUDE.md § Artifact Standards requires it, so its recurrence is compliance, not duplication (verified mechanically: 62/62 flagged blocks sat inside a --- + name: template). And 5 of the 7 'restatements' were false too: python-contract-test cites CLAUDE.md correctly; opentofu-module's flagged string was a Terraform tenant_id regex, not the component-naming regex; sdlc-config-management constrains product_slug and explains why; glossary-management is the allow-listed home for definitions; integration-design merely uses canonical terms, which CLAUDE.md MANDATES.
- WHY NOT JUST DECLARE P4 EMPTY: a report where 60+ of 62 findings are noise is worse than no report — a genuine future duplication would hide in the noise and the backlog would read as permanently unfinished. Charter decision #5 makes the linter the campaign keystone; a keystone with this precision is not doing its job.
- FOUR PRECISION FIXES to scripts/lint-duplication.py: (1) #1193 exclude artifact-frontmatter templates from corpus assembly, suppress findings that already cite their CLAUDE.md home, match the LITERAL naming regex rather than any regex-shaped string (clusters 13->0, restatements 7->6); (2) #1203 the methodologies detector tested 'methodologies present' and 'enforcement marker present' INDEPENDENTLY across a whole corpus, so glossary-management's 407-line file matched on an unrelated postmortem row 37+ lines away — fixed with an enforcement-marker CO-LOCATION WINDOW calibrated against CLAUDE.md itself (3->2); (3) #1205 the Ubiquitous Language detector was INVERTED — it flagged term USAGE, which CLAUDE.md mandates, instead of glossary REPRODUCTION — rewritten to detect the term->definition shape with the quorum calibrated on measured data (33 defined terms in the glossary home vs 4 in the highest non-home skill) (2->1).
- THREE SKILL ATTRIBUTION FIXES (the only genuine findings): react-e2e-testing (#1195) and react-observability (#1197) now attribute their frugality application to CLAUDE.md § Budget and Frugality; sdlc-config-management (#1201) attributes the component-naming pattern to CLAUDE.md § Naming Conventions, which is correct there because product_slug prefixes every artifact ID the plugin emits.
- ONE DELIBERATE SURVIVOR: opentofu-module. Its child (#1199) REFUSED the attribution fix and was right to — the JSON-Schema pattern governs a service name inside a GENERATED PRODUCT, so citing plugin-internal CLAUDE.md governance there would be wrong. It is a true-positive-by-text / false-positive-by-meaning coincidence, correctly explained rather than silenced. The child instead fixed a real factual error it found: the description claimed a Kubernetes rule the regex does not implement. restatements: 1 is an ACCEPTED FINDING, not remaining work — do not 'fix' it.
- FINAL COUNTS: recurring-block clusters 13 (62 skills) -> 0; CLAUDE.md restatements 7 -> 1; lint-duplication test assertions 20 -> 73 (the precision rules are pinned by tests, so a regression is caught, not re-litigated).
- generated/duplication-report.md regenerated and committed once at close-out (every child deliberately avoided committing it so it would be written a single time from the final linter). Housekeeping: CLAUDE.md § Governance gate now describes lint-duplication's ACTUAL contract instead of 'the P4 backlog'; README active-work banner; CHANGELOG [Unreleased] entry; this chunk + the campaign block's p4_complete record; and the charter's ~120-186 estimate is VISIBLY corrected (struck through with the audit finding beside it) in §4 and §10 rather than quietly overwritten, so a cold-start agent cannot re-derive the disproven plan.
- Verified: python3 scripts/lint-duplication.py -> 0 clusters / 1 restatement with the committed report matching; bash scripts/lint-all.sh -> exit 0 across all 5 steps; sdlc-context.json parses.
